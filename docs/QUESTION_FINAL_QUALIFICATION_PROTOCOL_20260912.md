# Final configuration qualification — frozen before inference

The prior continuation stopped at 48/48 calls (150 including the original audit).
Its fresh author matrix used intermediate policy 6, not the finished policy 8.
This is a separate, final window of at most **24** calls, with no replenishment
for failed, malformed, rejected or low-yield cases. Cumulative ceiling: **174**.

Run the four committed `subject_reference_final.json` requests once with each of
the deployed Nova Lite and Kimi author models. Each asks for two questions: mixture
replacement (difficulty 4), Python closures (3), relative-clause meaning (3), and
fictional transit connections/days (2). The fixtures mix source-free requests,
attached study notes, learned rules in goal prose, and facts in skill/objective
details. Alternating author order, one generation pass and three actual calls per
job include the existing possible JSON repair within the same cap. Stop each job
on transport failure. Use the actual production subject-reference solver (policy
8), existing reviewer-written full-choice teaching, legacy authors, and native
Sonnet solver/reviewer. No semantic prompt overrides or extra rejection/retry layer.

The proposed SAM, deployment script and workflow now default only verification to
native. The matched eight-draft transport control recovered five correct keys and
four fully supported final explanations from zero legacy returns. The subsequent
21-call fresh matrix produced 12/12 valid native verification envelopes with both
legacy authors. This supports fixing the recurring envelope mismatch, while the
recorded false teaching and reason-length failures remain semantic/application
limits. Unset standalone process environments retain the historical inheritance
behavior; explicit legacy/inherit overrides remain available. No deployed setting
has changed. A different verifier still requires compatibility qualification.

Before accepting the configuration, read every actual prompt/response and all
transformations, including failed and repaired drafts. Assess every key, ambiguity,
main explanation, choice explanation, distractor, and requested difficulty; record
unsupported claims and unnecessary rejection. Independently calculate mixtures,
execute literal Python snippets, use the supplied language rules (consult primary
reference material for uncertainties), and enumerate transit paths/days. Check
exact reference/stimulus preservation and real bank-preparation/JSON transport.
Record useful slots, all provider calls and measured pipeline latency without
claiming API/queue timing or a population error rate. Keep captured evidence
immutable; assessments are separate. Do not repeat this matrix until it passes.
