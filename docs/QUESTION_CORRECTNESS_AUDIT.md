# Question correctness audit — September 5, 2026

## Release status

The repository has stronger validation and better diagnostics, but the correctness issue is **not fully resolved**. No backend deployment or production model promotion was performed during this audit. Model agreement remains insufficient evidence of correctness. Existing app-blocking and break rules were not changed.

Latest September 9 native-format regression: the [ten-call downstream probe](QUESTION_NATIVE_STAGE_PROBE_RESULTS.md) covered all seven previously malformed checker inputs plus three exact repeats. All ten responses passed native schema/adaptation and the stated stage checks without increasing output tokens or deadlines. Exact offline application-policy replay nevertheless accepted a spreadsheet item with an unstated third operand in both reviewer repeats. Map solvers also approved conflicting premises and a condition imported from a choice. Native formatting addresses the observed response-format failure on this selected set; it does not resolve silent premise substitution. Independent subject assessment was frozen before model outputs. The integrated backend passed 1,046 tests and the subsequent targeted probe/observer/runtime suite passed 59. No production mode, model or deployment changed.

Latest September 9 author-capacity evidence: the [six-call explanation trial](QUESTION_MAIN_CAPACITY_RESULTS.md) produced all twelve requested raw questions. Both arms had one complete-content item out of six; widening main guidance did not increase that count. A supported 529-character SQL explanation establishes a narrow retention benefit under a virtual 900-character allowance, while wrong exposure answers, false interval teaching and missing type context remain. All six calls ended normally far below the output ceiling. Two masked assessment phases and independent replay/summary checks preserve the distinction between content quality and length admission. The source milestone passed 980 backend tests. No production limits, models, verification policy or deployment changed.

Latest September 9 qualification: the [explicit task-obstruction candidate](QUESTION_TASK_OBSTRUCTION_RESULTS.md) completed 20 calls after an equivalent [nullable-enum schema correction](QUESTION_TASK_OBSTRUCTION_SCHEMA_FIX.md). All five valid controls were retained and four of five defective items were rejected for actual defects. The solver still rationalized dough's false premise, and teaching still missed butter's language-dependent generic-reference defect. All 20 responses met native and application schemas; no output ceiling was reached. The frozen complete criterion failed. Source validation passed 973 tests. Earlier operational failures remain recorded separately; no production integration or deployment followed.

Latest input-isolation result: the [split sourced-review comparison](QUESTION_SPLIT_EVIDENCE_RESULTS.md) completed 12 calls with the same four questions and previously acquired passages. Answer solving was blind to authored teaching and the discovery hypothesis, but still defended the dough's false premise. Butter's misleading main remained undetected; valid hotel/plant controls were excluded for non-defect notes. All 12 responses met native schemas, eight met application contracts, and no candidate item was eligible. The complete diagnostic criterion failed. Exact no-client replay and 38 committed source hashes verified; the full backend repeat passed 954 tests. No production integration or deployment followed.

Latest acquired-evidence result: the [separate citation-discovery follow-up](QUESTION_CITATION_DISCOVERY_RESULTS.md) completed 12 calls, six successful page acquisitions and eight schema-valid reviewer responses. Four responses still failed exact target/quotation or record-count checks. Evidence resolved uncertainty on the supported plant control, but did not demonstrate an additional justified catch of either known defective English item; source-backed reviewers still misread relevant evidence. The complete criterion failed. Source validation passed 937 tests; no production integration or deployment is claimed.

Latest tool-assisted attempt: the [claim-directed discovery run](QUESTION_CLAIM_EVIDENCE_INTERFACE_RESULTS.md) completed eight calls and six native searches, but JSON-only discovery responses supplied no native citation URLs. No page could be acquired and all four evidence arms were skipped. Three source-free reviews also failed the response contract. This identifies a missing citation handoff, not a correctness improvement; the exact failed comparison is preserved.

Latest admission safeguard: [strict semantic response envelopes](QUESTION_SEMANTIC_RESPONSE_ENVELOPE.md) prevent solver/reviewer parsing from discarding surrounding prose and retaining only a structured approval. All four semantic decision parsers now require a complete JSON object or a sole fenced object. Malformed responses fail as format errors; this can also withhold valid items sharing a batch. Contradictions wholly inside valid JSON and confidently wrong model judgments remain unresolved. No live correctness improvement or deployment is claimed.

Latest author-prompt comparison: a [focused application trial](QUESTION_FOCUSED_APPLICATION_RESULTS.md) replaced construction instructions while preserving models, context and checks. Fourteen calls produced 12 candidates. The focused arm returned 3/6 versus the balanced arm's 2/6, but two focused returns retained misleading generic-reference explanations. All focused items were independently rated level 2, and all 12 mains exceeded the author instruction. The solver correctly blocked a question with missing elevation direction; the auditor rejected another question's unsupported teaching, with imperfect reasons. Comparable choice dimensions did not reliably establish premises or supported teaching. The source milestone passed 895 backend tests. The variant remains opt-in; no demonstrated correctness improvement, default promotion or deployment is claimed.

Earlier author-model comparison: a [paired Kimi K2.5 versus Opus 4.6 trial](QUESTION_AUTHOR_MODEL_COMPARISON_RESULTS.md) changed only the author under the current opt-in path. Twelve calls produced 12 candidates across the same three goals. Independent assessments supported 2/6 Kimi keys and 5/6 Opus keys; the runtime returned 4/6 and 2/6 respectively after six oversized-main exclusions. One returned Kimi explanation retained the misleading generic-reference claim, and other questions still had missing conditions or teaching qualifications. All mains exceeded the 320-character author instruction, and only one raw item combined a supported key, independently assessed difficulty 3 and plausible distractors. Neither arm met the prospective full criterion. No output ceiling was reached; no general superiority or release readiness is claimed. The source milestone passed 892 backend tests; no default promotion or deployment followed.

Latest implementation: an opt-in [authored worked-explanation contract](QUESTION_AUTHORED_SOLUTION_CONTRACT.md) preserves the author's complete teaching and makes the final reviewer audit that exact text without writing replacement feedback. In its [earlier fresh three-goal trial](QUESTION_AUTHORED_SOLUTION_FRESH_RESULTS.md), six raw candidates yielded two returned English questions after five live calls. Four candidates failed length checks; the returned items were independently below the requested challenge, and one main explanation retained a misleading generic-reference claim. The two unreviewed contour candidates also had defective authored content. No output ceiling was reached. This path alone assigns policy revision 3; defaults and claim policy stay at revision 2. The backend suite passed 889 tests. No default promotion or deployment followed.

Latest September 8 evidence: a [four-call reasoning comparison](QUESTION_REASONING_RECHECK.md) reused the exact five fresh questions from the [worker-setting qualification](QUESTION_RUNTIME_QUALIFICATION.md). Both solvers selected identical keys. The disabled final review failed JSON parsing; adaptive/high returned all five but retained unsupported teaching and introduced a survey response-rate error. Adaptive avoided the earlier false prevalence counterfactual, but did not meet the prospective complete-teaching criterion and used more tokens and time. No production defaults changed. The full repeat backend run passed 864 tests; an initial unchanged cleanup-test failure and its bounded diagnostic checks are recorded separately.

Earlier September 8 evidence work: [public-source acquisition](QUESTION_SOURCE_ACQUISITION.md) now captures actual reference text with exact hashes and explicit omissions. A live four-reference run exposed a generic omitted-HTML-tag extraction bug, which was fixed against the saved bytes. An [immutable acquired-source adapter](QUESTION_ACQUIRED_SOURCE_REVIEW.md) enforces exact quotations without treating them as proof. In a [three-goal fresh trial](QUESTION_ACQUIRED_SOURCE_TRIAL.md), Python yielded a supported level-3 item, history was format-rejected and independently had overbroad feedback, and the accepted seasons item was independently rated level 2 with incomplete source coverage for some background facts. Five calls completed, no output ceiling was reached, and no general accuracy gain or release readiness is claimed. At that milestone, backend validation passed 826 tests with one existing optional-runtime skip.

A separate [read-only deployment snapshot](evidence/deployed-backend-correctness-20260908.json) confirms that the known TestFlight API/worker packages still run policy revision 1: the inspected verifier files match `601d273`, while complete-choice policy 2 is absent. Typed outcomes, nonempty limitations and exact alternative-key vetoes are already live; the old assumptions-only account is stale. The worker uses Kimi K2.5 and Sonnet 4.6 review with thinking disabled and a 6,000-token ordinary ceiling. The Opus source-backed trial and newer repository safeguards are not deployed behavior.

Latest September 8 runtime change: [complete-choice solving and policy revision 2](QUESTION_COMPLETE_CHOICE_RUNTIME_POLICY.md) now gate generation on explicit judgments for every offered answer. Zero/multiple supported choices, uncertainty and exact key disagreement cannot be overridden by final review. This general interface replaces free-prose answer matching in generation while preserving the historical evaluator below. Confident wrong judgments or contradictions within their reasons remain possible; this is not a semantic-correctness guarantee. Fresh verified practice requires the new provenance, while stored questions and attempts retain their history.

A [fresh complete-content audit](QUESTION_FRESH_AUTHOR_IMMUTABLE_RESULTS.md) also found that the immutable reviewer approved a false CSS explanation with no reported issue, despite ample tokens and thinking enabled. A fixed native WebKit observation corroborated the error. The sound English control was correctly rated below the requested challenge. This is evaluation evidence, not a CSS-specific product fix or a general accuracy estimate; the immutable auditor remains experimental.

September 6 follow-up: [the independent solver's declared outcome is now enforced in code](QUESTION_SOLVER_OUTCOME_GATE.md). A live four-case diagnostic retained both valid keys but still accepted both known invalid questions because the solver incorrectly declared them `resolved`. This closes a control-flow gap without qualifying the system for release. The historical measurements below remain unchanged.

A further [resolved-limitations veto](QUESTION_SOLVER_LIMITATIONS_GATE.md) prevents reviewers from waiving explicitly reported obstacles. An answer-only contradiction still bypasses this field check: a report labeled `resolved` with empty limitations can describe an impossible premise and reach a reviewer that approves a positive key. The documented offline replay records that remaining path. The latest recorded invalid cases still have empty limitations and are unaffected. Offline replay also demonstrates a conservative loss for harmless scope notes; no new live accuracy improvement is claimed.

A September 8 [current-source mock](evidence/answer-only-gap-20260908.json) independently reproduces that remaining answer-only acceptance, including verification version and policy revision 1. The corresponding declared-outcome, nonempty-limitations and exact-rival controls block review. A separate [five-case immutable teaching audit](QUESTION_IMMUTABLE_REVIEW_FOLLOWUP_RESULTS.md) retained three sound fixed controls and rejected both defective items. On the all-pairs item, its model still marked the wrong answer and every explanation supported, while also reporting the decisive output-size problem. The prototype's nonempty-issue veto prevented return. This supports enforcing objections in code; it does not close the current solver prose gap or qualify production integration.

An additional exact-answer veto now blocks review when the blind solver's answer exactly names a different offered choice. An approving reviewer cannot override that declared disagreement. Three new [mock regressions](../backend/bedrock-question-service/tests/test_solver_exact_answer_gate.py) cover rival answers, exact agreement, and ordinary nonmatching summaries; the full 628-test backend suite completes without failures, with one existing optional-runtime skip. This adds no model call or semantic classifier, and does not close the answer-only impossibility path above. A mistaken solver can still cause rejection of a valid item. No live accuracy improvement or deployment is claimed.

[Verification policy freshness](QUESTION_VERIFICATION_POLICY.md) separately records execution of the current gates, filters stale server inventory and claim replays for opted-in clients, and requires current provenance in new Pro practice from first use. Old questions and attempts keep their historical content and grading; obsolete blocked bank contexts reset, while ambiguous transport retries retain their idempotency keys. The complete iOS suite passes 985 tests. Rollout requires the backend before the new client; older installed clients retain legacy behavior. This provenance does not certify semantic correctness or close the answer-only contradiction above. No deployment occurred.

A September 8 [fixed-solver compatibility comparison](QUESTION_SOLUTION_COMPATIBILITY_RESULTS.md) tested a separate, narrower mapper in sixteen live calls. It retained four valid controls but also approved the same defective all-pairs key as the current reviewer, producing zero additional vetoes. Both calls had the full preserved caveat in the input. The mapper also mislabeled a proved-impossible `resolved` record as coherent, although its four correct choice refutations blocked that item. This additional call is not being integrated into production; agreement and structured labels did not establish the required support.

A subsequent [fresh complete-MCQ solver comparison](QUESTION_COMPLETE_SOLVER_RESULTS.md) completed sixteen calls. Providing choices and a per-choice contract restored two valid option-dependent questions and two ordinary negative answers that the current first gate excluded. It correctly distinguished zero/multiple-answer grammar cases but still approved the invalid all-pairs key in fresh inference; both solver arms made the same linear-time error. The candidate also incorrectly excluded constant space from an O(n) bound. Five correct valid mappings did not establish entirely supported reasons. The frozen follow-up criterion failed, so no final-review follow-up or runtime promotion occurred.

An [artifact-first authoring trial](QUESTION_ARTIFACT_AUTHORING_RESULTS.md) then requested two Python and two HTML exercises whose keys would be derived from exact native observations. Python returned two structurally eligible proposals; HTML timed out at 100 seconds, stopping the trial before any generated artifact was executed. Independent offline assessments found both Python keys clear, one question below the target difficulty, and one distractor explanation ambiguously describing reference sharing. The prototype's exact construction, transport and replay checks are implemented, but no fresh native-bound question, generalized correctness improvement, deployment or runtime promotion is claimed.

A [separate native follow-up](QUESTION_ARTIFACT_NATIVE_FOLLOWUP.md) later observed those two unchanged Python artifacts using two managed sessions and no additional model calls. Both native-derived keys matched the previously frozen manual answers; exact lifecycle replay confirmed cleanup and unchanged displays. This qualifies two literal artifact/key bindings, while preserving the original failed trial, missing HTML candidates, difficulty/feedback findings, and lack of production integration.

A subsequent [source-first authoring comparison](QUESTION_SOURCE_AUTHORING_EXPERIMENT.md) generated twelve candidates across plant propagation and accessible forms. Giving sources to the author had mixed content results and still allowed unsupported explanations and missing conditions. Neither arm produced a candidate with both complete supported reviewed content and independently assessed difficulty at least 3. This small feasibility experiment does not qualify a release or establish production accuracy.

Further work added an unintegrated [Boolean rule-question constructor](QUESTION_RULE_CONSTRUCTION.md): it renders the complete premises and feedback from one formal object and exhaustively checks the key and counterexamples. This is one additional, bounded inference mechanism. It does not establish real-subject rule accuracy, useful difficulty, general question coverage or a production fix. A separate [web-grounding probe](QUESTION_WEB_GROUNDING_PROBE.md) demonstrated search access while leaving raw passages hidden and still producing an inaccurate explanation.

September 7 follow-up: backend normalization now preserves meaningful whitespace in adaptive mistake stems and answer text. Previously it flattened multiline code and could collapse a selected answer and a distinct correct answer into the same string. Regression checks reproduce the loss before the fix and confirm preservation through both initial and retry author payloads afterward, with the existing length limits still enforced. The backend suite passes 501 tests with one existing skip. This fixes supplied learning context; it does not establish model accuracy, learning outcomes, or deployment. As of that September 7 snapshot, the separate [320-versus-1200-character stem experiment](QUESTION_STEM_LENGTH_EXPERIMENT.md) awaited renewed AWS authentication with no calls dispatched. That status is superseded by the [September 8 partial live results](QUESTION_STEM_LENGTH_RESULTS.md).

A client integrity follow-up found another concrete path: backend normalization can move the correct choice first after checking for redundant trailing choices, making real stimulus lines resemble copied choices when iOS repeats that cleanup after review. In a [shared synthetic regression](../backend/bedrock-question-service/tests/fixtures/reviewed_stem_contract.json), the old client helper removed all four lines of a valid Python script from a reviewed 123-character question. All text met current limits; fixed solver/reviewer replies isolate this transport defect from model judgment. The client now preserves the received stem exactly for verification version 1, while retaining length, structural-content, duplicate, and answer checks. Minimum content length is validated separately so blank-line padding cannot admit an empty or too-short stem. Legacy questions retain their existing cleanup. Verification passed 88 selected iOS simulator tests and 502 backend tests with one existing backend skip, plus independent review. This prevents this post-review deletion; it does not prove that the model's original question or answer was correct. No deployment or live model call was part of this fix.

September 8 follow-up: the [stem-length comparison](QUESTION_STEM_LENGTH_RESULTS.md) completed both three-question HTML arms, then stopped when the first chess author call timed out. All six HTML keys and returned main/all-choice explanations were independently supported. Each arm had two of three items rated at least difficulty 3 and two of three with adequate distractors; the combined count was 1/3 baseline and 2/3 expanded. A separate assessor's lower API-item difficulty ratings reduce those combined counts to 0/3 and 1/3. One raw explanation invented a missing value, and one internal solver invented a required-constraint failure; neither claim appeared in returned feedback. The sole 327-character stem could retain all its premises at 296 characters by removing introductory wording. The incomplete trial establishes neither the necessity of longer stems nor a generalized correctness improvement, and no deployment or model promotion followed.

A later [independently solved answer construction trial](QUESTION_SOLUTION_CONSTRUCTION_RESULTS.md) separated task authorship from solving, preserved the solver's exact answer, and allowed a third call to add only distractors and feedback. Seven calls completed normally across four cases, but six field-length violations left zero admissible questions and zero reviewer calls. Blinded inspection of the two available raw MCQs found the familiar incorrect all-pairs answer and a best-listed photography diagnosis with missing causal controls; the other two cases had only tasks. Output-token limits were not reached. Freezing the solver's key prevented replacement without making that key correct. This prototype remains outside production; no repair, retry, deployment or model promotion followed.

## What is holding the system back

1. **Confident substitution of a familiar problem.** Reviewers sometimes answer a standard textbook problem instead of the actual wording. One failing fixture asks to enumerate all matching pairs in linear time; the reviewer approves a method for finding whether a pair exists. Another assumes an image-editing order can recover detail without establishing whether the source data still contains that detail.
2. **Author and reviewer mistakes can agree.** More reasoning and a larger available model did not reliably eliminate these shared mistakes. Solving the stem without options helps, but does not guarantee independence of knowledge or assumptions.
3. **Difficulty labels overstate the work required.** Many authored level-3 items are independently rated level 2. Previously, another attempt received excluded stems but no explanation of the rejection, so it repeatedly produced similarly easy items.
4. **The call budget did not fit the new pipeline.** Authoring, independent solving, and final review take three calls. A five-call job could start a retry it could not finish checking. The proposed default is now six calls, with the existing deadline and daily quota still enforced.
5. **Malformed output is a separate problem.** Duplicate JSON keys can silently overwrite an answer; oversized feedback can discard a valid item. These need deterministic parsing and clear length constraints, rather than more model confidence.

## Live comparisons

These are small, deliberately difficult control sets, not estimates of production accuracy. The original set contains seven invalid questions and three valid controls. Each row reports one bounded run; repeated prompt experiments varied.

| Configuration | Original 10 controls | Additional 8 controls |
| --- | ---: | ---: |
| Previous reviewer prompt, Sonnet 4.6, ordinary 6,000-token budget | 6 passed | Not run |
| Previous reviewer prompt, Sonnet 4.6 adaptive/high, 16,000-token budget | 5 passed, including one timeout as failure | Not run |
| Previous reviewer prompt, Opus 4.6 adaptive/high, 16,000-token budget | 7 passed | Not run |
| Revised independent solver + final audit, Opus 4.6 adaptive/high | 8 passed | 8 passed |

The final 18-control run retained all seven valid controls and rejected nine of eleven invalid items. The two remaining false acceptances were `all_pairs_output_bound` and `raw_highlight_recovery_unsupported_sequence`. A subsequent generic counterexample/evidence prompt did not resolve them.

All ten baseline responses ended normally, using 277–1,256 output tokens. That directly argues against the 6,000-token output cap causing these particular correctness failures.

Kimi thinking was also tested with the documented sampling settings. A small fictional-rule generation probe still produced inconsistent answers and duplicate properties. DeepSeek V3.2 was tested on the two unresolved items plus a valid control: ordinary mode passed two of three, and thinking mode passed one of three. Neither established a reliable replacement. Newer GPT and Claude model invocations were denied by this AWS account despite appearing in its model catalog; this was not an expired sign-in.

A final one-job generation smoke test used Opus 4.6/high, five requested items, minimum difficulty 3, and the proposed six-call allowance. Music retained two of five questions after six calls (179 seconds). A fictional-rule goal had three approved questions, then lost them when a top-up failed (247 seconds). That discovered a separate partial-inventory bug, now covered by regression tests: ordinary provider failures preserve earlier verified questions, while durable quota failures still propagate. These runs did not pass the full-bank release criterion, and the final preservation fix was verified locally rather than claimed as another successful live run.

## Changes implemented

- Separate configurable ordinary and reasoning output budgets: 6,000 and 16,000 by default. Kimi and supported Claude reasoning settings are configurable; thinking defaults remain disabled pending a qualifying configuration.
- Reject responses stopped by the output-token limit, even when their text parses.
- Reject duplicate JSON properties instead of silently accepting the final key.
- Solve stems before revealing choices. Require explicit missing-assumption reporting and reject items whose solution needs extra factual premises.
- Audit the supported remainder with hidden author keys, explanations, and difficulty labels; preserve item indexes when filtering a mixed batch.
- Shorter author instructions and bounded teaching feedback; unchanged four-choice client contract.
- Retry feedback carries only this attempt's finite rejection counts. It asks for genuinely harder cognitive work when difficulty is too low, without lowering the learner's target. Feedback currently stays within one invocation, not across separate queue jobs.
- Six-call background-job default supports two complete passes when time permits. Do not spend calls on a pass that cannot complete verification.
- Preserve earlier verified inventory when an ordinary provider failure interrupts a top-up; still fail closed when no verified inventory exists.
- Evaluations retain model settings, prompt hashes, token use, stop reasons, rejection counts, and earlier inventory after a later failure. Invalid JSON or a timeout does not receive credit for detecting a bad question.

## Remaining work before release

A qualifying candidate must reject the unresolved controls, retain valid controls, and fill banks at the requested challenge level across fresh goals and repeated runs. The production source path still supplies selected source text, without independent web retrieval or execution of a solution. Subsequent grounding, execution and construction prototypes described above remain unqualified for production integration. Increased model power alone has not passed this release bar.

The tests here exercise live provider calls and local orchestration, not deployed queue delivery or learning outcomes. Some live comparisons used a 100-second read timeout; the worker setting is 75 seconds. A successful local response is therefore not proof of production latency readiness.

Local verification passed 261 backend unit tests, Ruff, deployment-script checks, and SAM template linting.

## Research used

- [Moonshot Kimi K2.5 documentation](https://github.com/MoonshotAI/Kimi-K2.5): thinking-mode sampling settings.
- [AWS Kimi K2.5 model card](https://docs.aws.amazon.com/bedrock/latest/userguide/model-card-moonshot-ai-kimi-k2-5.html): endpoint limits.
- [AWS Claude adaptive thinking](https://docs.aws.amazon.com/bedrock/latest/userguide/claude-messages-adaptive-thinking.html): supported reasoning request fields.
- [Anthropic prompting guidance](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices): clear tasks and explicit output requirements.
- [CRITIC](https://arxiv.org/abs/2305.11738): external tool feedback as a mechanism to evaluate, rather than relying only on another model judgment.
- [RAGTruth](https://aclanthology.org/2024.acl-long.585/): supplied references do not ensure every generated claim is supported, and detecting unsupported spans remains difficult. This supports retaining actual evidence and assessing keys and teaching fields separately; source identity alone is not proof of entailment. Its tested models and tasks do not estimate this app's error rate.
- [Large Language Models Cannot Self-Correct Reasoning Yet](https://proceedings.iclr.cc/paper_files/paper/2024/hash/8b4add8b0aa8749d80a34ca5d941c355-Abstract-Conference.html): limitations of unaided self-correction; this is not evidence about the exact models tested here.

Synthetic control fixtures are in `backend/bedrock-question-service/evals/fixtures/question_verification_cases.jsonl` and `question_verification_holdout.jsonl`. The latter covers arithmetic, SQL, sewing, irrigation, and newly supplied fictional rules, without subject-specific runtime branches.
