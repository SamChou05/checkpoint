# Programming-result pair distinction — draft only

No runtime source, schema, historical evidence, or provider state has changed. This is a proposed prompt-only diagnostic, not a frozen plan or an execution authorization.

## Observed mechanism

In the immutable-main mixed capture, call 6 (`complete_choice_solver_v5_n5`), solution 3 has a=`True`, b=`0`, c=`'hello'`, d=`False`. It correctly supports only the string answer to `What does the expression 0 or 'hello' evaluate to in Python 3?`. Pair bd nevertheless declares equivalence: its reason acknowledges different types and then uses numeric equality to collapse these wrong proposed results. The production pair gate faithfully rejects this declaration. This is a semantic false veto, separate from schema failures, explanation bounds, or answer shuffling.

`complete_question_solution.py:113–125` replaces the older representation paragraph in the slot prompt. The replacement permits representation distinctions only when the exact written form is itself the task, and instructs the model to compare underlying value even when a display convention is given. That guards against admitting numerical aliases merely because their notation differs. It does not explain that a programming result's type can be part of its meaning, rather than a cosmetic display convention. The observed reason fits that omission. It does not establish that this paragraph caused the failure; one stochastic response cannot isolate that effect.

Python documents that `or` and `and` return an operand, that Boolean and numeric objects can share a truth value, and that mixed numeric equality compares values. Those relations do not establish interchangeable typed results. [Python Boolean operations and numeric types](https://docs.python.org/3/library/stdtypes.html#boolean-operations-and-or-not). For these fixed fixtures, local checks confirm that `0 or 'hello'` is a string, `0 and 'ready'` is an integer, and integer zero has a different exact type from Boolean false despite equality. The same checks also confirm that `0` and `0x0` denote the same integer result. See `offline-checks.json`; no model output was executed.

## Narrow candidate boundary

Append only `candidate-addition-draft.txt` immediately after the existing paragraph ending `preserve the requested literal difference.` Retain the existing numerical/notation rule and every other instruction. The addition distinguishes actual programming results and output, while explicitly retaining equivalence for different spellings/expressions with the same relevant value and type. When a task explicitly asks only about magnitude, equality, or truth value, that property remains the comparison target.

This is not a Python interpreter, a generic semantic compiler, an automatic type detector, or permission to treat every differently written option as distinct. Neither schema nor deterministic gate behavior changes. The historical failed capture remains failed.

## Prospective diagnostic proposal

Use the eight questions and all gold in `controls-draft.json`. The independent reviewer and root approved this packet unchanged; `independent-gold-review.json` binds that assessment, including its not-fully-blinded limitation. It contains five eligible controls and three defective controls, with 32 independent choice labels and all 48 unordered pair labels. All offered strings remain exact. The captured question is familiar. The seven other controls are newly drafted matched controls; shared stems and matched variants can aid cross-item comparison, so this is diagnostic evidence rather than independent generalization evidence.

- Batch 0: captured `or` result; fresh `and` result; wrong Python integer aliases `0`/`0x0`; `str(2)` string result.
- Batch 1: wrong numeric aliases `2`/`two`; numerical half `1/2`/`0.5`; an explicit decimal-notation task with the same half choices; the distinct-choice arithmetic counterpart.

The first batch tests programming result semantics without granting blanket spelling immunity. The second tests that the proposed exception does not undo ordinary numeric duplicate rejection, and that an actual notation task still retains its meaningful distinction. The half-value question deliberately has two supported choices; its rejection is not credited as evidence that the pair label is correct unless that label itself matches gold.

Propose exactly four calls: baseline batch 0, candidate batch 0, candidate batch 1, baseline batch 1. Each baseline/candidate pair must have byte-identical subject input, schema, model and inference configuration; only this single system-prompt insertion may differ. Use actual `build_solver_prompt(..., audit_choice_pairs=True, choice_slots=True)`, its key-independent choice placement, and `SolverSlotContract(4)` with the existing native wrapper. Keep explicit keys, gold, source provenance, novelty labels and prior verdicts out of provider input. Both arms receive the same prospective common goal and empty sources/skill map from the packet; no historical scope is silently rewritten.

Proposed settings match the last solver: Sonnet 4.6, adaptive/high, maxTokens 16000, no temperature/top-p, read timeout 100 seconds, connect timeout 3 seconds, SDK total attempts 1. No automatic retry, top-up, author, final reviewer, question-bank write, deployment, or source editing. Retain a maximum four-call ceiling and each planned arm/case denominator on any failure. Continue the fixed independent calls after ordinary provider, safety, timeout, malformed-response, or late-output failures; give the affected four cases zero credit. Stop globally only for setup/credential/source/request/capture integrity or an actual reservation/call-ceiling violation. A normal response later than 100 seconds loses structural credit for that call but does not stop later calls. There is no separate global elapsed-time cap. Record elapsed time, safe provider error details and usage when available; never save hidden provider reasoning. Bind source, controls, candidate text, harness and exact requests before any future dispatch. The harness uses the permanently frozen OLD service at `/Users/samchou/.codex/worktrees/question-reliability-investigation/Checkpoint/backend/bedrock-question-service`; the new integration checkout is not a runtime dependency. The harness pins all used service modules, helper, controls, review, protocol, test source, and exact four requests before and after dispatch. No account activation is performed.

Each arm has a fixed denominator of 8 eligibility decisions, 32 choice labels and 48 pair labels. Report retention of all five valid controls and exclusion of all three bad controls separately. Require complete local/native identities and existing nonempty reason bounds (choice 600, pair 240), normal end-turn and the 100-second bound for structural credit. Missing or malformed batches receive no semantic credit; uncertainty does not satisfy a gold label. Audit each visible reason for factual correctness and the actual pair endpoints, separately from learner eligibility. Do not turn harmless private wording imperfections into a new learner-content contract; report material inconsistencies explicitly.

The proposed candidate clears this small diagnostic only with 8/8 eligibility, 32/32 choice labels and 48/48 pair labels, without a structural failure. Compare the baseline mechanically rather than assuming it reproduces the historical mistake. Even a clean candidate result would not qualify all topics, complete worker yield, deterministic model decisions, or rollout.

## Offline preparation performed

The unchanged current native adapter and pure complete-choice validator accepted evaluator-authored gold records for both four-item batches. Their actual rejection reasons agree with all eight gold eligibility decisions: five retained, two duplicate-choice vetoes, one multiple-supported veto. These records are synthetic test inputs, not model judgments. The exact question input builder omitted all answer/gold fields, and removing the candidate insertion restores the original system prompt byte-for-byte. These checks establish fixture and scoring compatibility only; no provider requests have been dispatched.

Draft control SHA256: `2e0d6006cf7ab5371301609ff7bf0a9b029718880fc780c4292c564ce4a9626f`.
