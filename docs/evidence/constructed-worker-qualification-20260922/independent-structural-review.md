# Independent structural review

**Structural replay passes; the trial fails its frozen compiled-question quotas.** All 11 recorded requests, reservations, pass records, returned fields and source associations reproduce offline through the unchanged runtime. All 37 source/artifact pins match; the dedicated source remains clean at `0e4619f1696b848073f1a4d551c364ed3fbfbeb0`. No provider calls were made for this review.

| Job | Requested / returned | Calls | Worker seconds | Compiled / prose returned |
|---|---:|---:|---:|---:|
| Quantitative | 5 / 5 | 3 | 53.436100 | 0 / 5 |
| Python | 5 / 5 | 3 | 61.407327 | 0 / 5 |
| Mixed | 5 / 4 | 5 | 92.328774 | 3 / 1 |

The original denominator remains **15**. Fourteen returns meet the total yield floor, and each job returns at least four. However, only **3/6 required compiled items** return, with **0/4 required quantitative-job compiled items**. All five quantitative-job author rows chose the ordinary prose branch of the native union. They passed the model solver and auditor; they never acquired compiler provenance. This is a routing/qualification failure, not corrupted sidecars or repaired failed specifications. The mixed compiled minimum of two is met. The remaining English assignment and all content requirements belong to separate semantic review; full qualification already fails regardless of that review.

All 11 provider calls end with `end_turn`, pass their exact serialized native JSON schema, adapt successfully, and pass the applicable strict local parser. The three solver calls cover **11 identities, 44 choice judgments and 66 pair judgments**. The four final audits cover **14 identities and 84 Boolean issue flags**, all flags false and all 14 dispositions positive. These are declarations with verified shape and binding, not independently established semantic truth. Only the observed counts are covered: author targets 5/2, solver counts 5/1, and audit counts 5/3/1.

Four author calls produce 17 raw rows: 13 prose and four task-only quantitative rows. All four typed tasks occur in the mixed job, and none includes supplied choices or a key. Three construct successfully and retain all five exact compiler fields through final release with policy 8. Each reproduces from its original flat task, translated full specification and final compiler output. All 11 released prose items retain their exact authored main, empty choice-feedback map and policy 7. The replay recreates actual retained verifier-object associations and current-pass source ordinals; this review does not infer memory identity from serialized text alone.

The three discarded raw attempts have distinct causes:

- One mixed typed task declares `(5 * 2 + x - 17) = 5` with `any_satisfying` over integers 1 through 10. Its only algebraic solution is 12; independent exact `Fraction` enumeration finds no feasible domain value. The actual constructor raises `no_answer`. This is an impossible declared task, not a missing supplied answer. No graph, equivalence, insufficient-distractor-pool or other compiler failure occurs in this capture.
- One mixed prose item survives sanitization but its main refers to shuffled choice labels. The actual immutable-content freeze rejects it before solving with `Teaching cannot reference shuffled answer labels.` This is a deterministic guard, not a model veto.
- The top-up repeats that rejected raw stem byte-for-byte. The sanitizer drops it as `duplicate_stem`; it is not rescued or restamped.

Thus 17 raw attempts account for 14 returns without hiding failures or changing the denominator. No submitted solver or auditor item is vetoed by its model. Compiler-proven items bypass only the model solver; all three still receive the final audit. Mixed ends after five calls, leaving one reservation. The existing conservative new-pass admission rule requires three, so no third mixed author pass begins. This is the normal call-budget stop, not a timeout or harness interruption.

Actual author requests use Kimi K2.5, disabled thinking, 6,000 tokens and temperature 0.2. Solver/auditor requests use Sonnet 4.6, adaptive/high, 16,000 tokens and no sampling fields. All dynamically reconstructed requests equal the capture. Every client records one SDK attempt, three-second connect timeout and 100-second read timeout; each call and each 240-second worker job finishes within its bound. The runtime factory retains its shrinking timeout rule, but reduced read timeouts are **not exercised** here because sufficient time remains. No provider/setup/native-format failure or global stop occurs. Eleven reservations equal 11 dispatches, below six per job and 18 overall.

Reported usage is **33,973 input tokens and 17,322 output tokens**, with no missing usage. Provider-call time totals 206.345339 seconds; worker time totals 207.172201 seconds across independent jobs. Eight reasoning blocks were omitted. No private reasoning text/signature was inspected or inferred. These are measured component costs and timings, not a price estimate or latency comparison with another run.

Replay used Python 3.12.11 and the pinned `mixed_probe.py`, with socket connections blocked and only the SDK factory replaced by exact recorded responses. Thirty-one real `check_plan` invocations verify all 37 pins before/after calls and completion. The actual `run_jobs` recreates every dynamic request and reservation, plus exact `passes`, `returned`, `returned_sources`, `returned_provenance`, call counts and statuses. Independent JSON Schema, native-adapter and local-parser checks supplement that replay. A fake clock is not claimed to reproduce the original elapsed times. Final source, plan and capture hashes remain unchanged.

Bindings:

- Plan: `29ca1b56074a64c9af5306c6c3e14880a6c7d9ed53bb065c6e2d0dd22f81b87c`.
- Capture: `49833e414ec775090b3408ff6b73d5b967c401fd64b6b4294d7533f1ac8c2658`.
- Detailed derived checks and reproduction procedure: [independent-structural-review.json](independent-structural-review.json).

No semantic approval, causal improvement, broad reliability rate, deployment readiness or change to the frozen criteria follows from this structural success. This review changes only its two derived files.
