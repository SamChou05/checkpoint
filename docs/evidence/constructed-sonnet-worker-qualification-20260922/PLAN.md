# Unfrozen Sonnet-author constructed-worker comparison

Preparation only. No provider dispatch, model activation, account change, bank write or deployment is authorized by these files. Root must review and freeze the exact plan before a new bounded execution. Preserve the completed Kimi baseline and all prior unsuccessful experiments unchanged.

The sole intended production configuration change is `BEDROCK_MODEL_ID`: `moonshotai.kimi-k2.5` → `us.anthropic.claude-sonnet-4-6`. Keep the same permanently frozen runtime source `0e4619f`, constructed_quantitative/native author contract and full existing prompt, authored_solution mode, baseline prose solver, immutable issue-flag audit, scope, jobs, criteria and budgets. Do not apply the unsuccessful prose-task-explicitness prompt addition. The new author inherits the already configured Claude adaptive/high settings: 16,000 shared tokens with no sampling fields. Global 6,000-token, temperature 0.2 and Kimi-disabled environment values stay unchanged; their model-specific interpretation differs. Solver/auditor requests remain Sonnet 4.6 with adaptive/high, 16,000 tokens and no sampling.

The exact baseline plan hash is `29ca1b56074a64c9af5306c6c3e14880a6c7d9ed53bb065c6e2d0dd22f81b87c`; capture hash is `49833e414ec775090b3408ff6b73d5b967c401fd64b6b4294d7533f1ac8c2658`. The harness checks those artifacts and all 37 baseline source pins, and includes them alongside its new source pins. It requires identical source root, jobs, limits, system/user prompts, native schemas and prospective criteria. Actual provider dispatch validates each stage's source-built request, model, configuration and shrinking timeout. Imports, draft building and preflight obtain no credentials or inference.

Motivation: the completed baseline returned 14/15, but independent review found one Python main explanation with a false execution narrative, leaving 13 sound items. It returned zero compiled numerical-job items and three compiled mixed-job items, failing the unchanged compiler quotas. Those results remain failures. Earlier Sonnet mixed-author-only evidence (14/15 in one review, 13/15 under stricter sensitivity) does not qualify this constructed author or a full pipeline. This trial asks whether the alternative author uses the existing task-only contract and meets complete-worker criteria; it does not establish a matched causal comparison, an isolated model-capability effect, universal accuracy or production rollout readiness.

Run the original three 5-item jobs once through the actual `_generate_sanitized_questions`, preserving all 15 requested slots. Runtime alone owns normal top-ups, deduplication and partial returns. Each job gets 6 durable provider reservations and 240 seconds; the global cap is 18 calls. The 100-second read ceiling shrinks with remaining job time. Connect timeout is 3 seconds, SDK total attempts is 1, and there is no fallback. Pure privately validated compiled rows skip the redundant solver, but all survivors still receive the final immutable audit; prose retains all blind choice/pair and audit gates. Actual source/object binding preserves all five compiled fields at policy 8 and prose exact authored main plus empty feedback at policy 7.

Unchanged prospective criteria: at least 14/15 returned and 4 per job within budget/deadline; at least 6 compiled total, including 4 numerical and 2 mixed, and at least 1 correctly assigned mixed English prose item. Every returned item's literal task, premises, unique exact key, all four choices and all six choice-pair meanings, plausibility, assignment, difficulty and all displayed teaching must be sound. Uncertainty fails. No hidden task, intended minimum, grammatical preference or omitted execution branch may be invented to rescue output. Model verdicts and structure do not replace independent learner-content adjudication. Missing, failed and unattempted slots stay in the 15-item denominator.

Preserve the reviewed guard implementation: actual client and native adapters; sticky credential, setup, configuration, source, request, provenance and budget stops; ordinary provider, safety, timeout, model and quota failures follow runtime behavior and later independent jobs continue. No harness rescue, retry, resume or overwrite. Source/capture pins are checked before and after dispatch and at completion. Record bounded errors and exact visible provider text; whitelist usage; omit reasoning, signatures and duplicate raw stopReason metrics; reject raw or decoded credential echoes before capture/adaptation, including duplicate JSON members. Missing usage remains unknown. No guardrail is configured in either frozen test; this does not authorize changing any deployed guardrail.

The offline suite retains the baseline constructor, provenance, partial/top-up, strictness, 18-call/deadline, source/capture drift and safe-capture tests. Added assertions bind every old pin, reject old-artifact drift, require only the author-model environment delta, and execute all three actual model roles through real source-pin rebuilding with fake responses. Fakes are transport tests, never model-quality evidence.

After completion, use the same key-independent blind worksheet helper to project returned stems/choices and lock independent literal-task solutions before revealing keys, mains or private judgments. Report yield, compiler uptake, full learner-content review, scope/difficulty, private stage diagnostics and latency separately. Prior outcomes and criteria remain immutable. Root owns freeze and any separately authorized dispatch; this directory remains draft-only until then.
