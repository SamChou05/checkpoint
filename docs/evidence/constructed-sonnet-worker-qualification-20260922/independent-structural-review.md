# Independent structural review

**Exact structural replay passes; this trial fails its frozen yield and compiler quotas.** The worker returns **9/15** requested items after one author timeout. All nine requests, reservations, pass records, return values and provenance bindings reproduce through the unchanged source with sockets blocked. All **44 pins**, including the **37 inherited baseline pins**, match. The dedicated runtime remains clean at `0e4619f1696b848073f1a4d551c364ed3fbfbeb0`. No provider call was made for this review.

| Job | Requested / returned | Calls | Worker seconds | Compiled / prose returned |
|---|---:|---:|---:|---:|
| Quantitative | 5 / 0 | 1 | 100.290039 | 0 / 0 |
| Python | 5 / 5 | 3 | 93.102481 | 0 / 5 |
| Mixed | 5 / 4 | 5 | 155.475229 | 3 / 1 |

Nine returns miss the required fourteen; the quantitative job misses the four-per-job floor. Three compiled returns miss the required six, and zero quantitative-job compiled returns miss the required four. The mixed compiled minimum is met. The mixed English assignment and all learner-content judgments belong to the separate independent content reviews. Full qualification already fails without relying on those judgments.

## Timeout and denominators

The first quantitative author call raises `ReadTimeoutError` after **100.168049 seconds**, with a 100-second socket read limit and one SDK attempt. No provider response, native JSON, stop reason or token usage is available. All five requested questions remain unobserved in the original denominator. They must not be classified as malformed schemas, invalid mathematical tasks or zero-token requests. The recorded elapsed time includes the timeout and surrounding overhead; it is a failed attempt, not a completed response within the limit.

The actual runtime ends that job after one reservation and continues the two independent jobs. There is no retry or rescue of the failed author call. Neither replay nor this report changes its outcome. The fast offline replay raises the same named exception to reproduce error classification and later control flow; it does not reproduce the network exception internals or live timing.

## Received structure and source preservation

All **eight received responses** end with `end_turn`, validate against their exact serialized native schema, adapt successfully, and satisfy the applicable strict local parser. This is eight valid received responses from nine dispatches, not nine successful calls. The two solver calls cover **six exact identities, 24 choice judgments and 36 pair judgments**. The three immutable audits cover **ten identities and 60 Boolean issue flags**: nine positive dispositions, one negative, 59 false flags and one true `distractors` flag. All ten main-support declarations are `supported`. Those labels are fallible model declarations; this review checks their structure and bindings, not their semantic accuracy.

The three successful author calls produce **12 observed rows**: eight prose and four task-only quantitative rows. These raw attempts do not replace the fifteen requested slots. All four typed tasks occur in mixed, contain no supplied choices/key, and construct successfully. Replaying each original flat task reproduces its full specification and all five compiler fields. No `no_answer`, graph, equivalence, insufficient-pool or other compiler error occurs among these observed tasks. Nothing can be inferred about the timed-out job's unobserved output.

Three compiled items pass the final audit and retain their exact five fields and policy 8. One compiled item is rejected by the audit's `distractors` flag. That veto is applied to the correct original source; its pedagogical legitimacy is outside this structural review, and the flag-only response supplies no rationale to reconstruct. Compiler proof bypasses only the model solver: all four compiled candidates receive the final audit.

All six returned prose questions retain their exact authored main, empty choice-feedback map and policy 7. Two additional mixed prose drafts are removed before solving. Direct replay of their content freeze raises `Teaching cannot reference shuffled answer labels.` at raw identities `mixed/0/4` and `mixed/1/1`. Both are deterministic format rejections, not model vetoes. Twelve observed drafts therefore account for nine returns, two pre-solver label failures and one final-audit veto; no solver item is vetoed.

The observer's actual retained verifier objects and current-pass raw/sanitized source relationships reproduce during replay. Exact serialized text alone is not treated as proof of memory identity. Compiled content is recompiled from its private trusted specification again at release, and prose receives no model-minted compiler stamp.

## Requests, budgets and usage

The only changed environment entry from the immutable Kimi baseline is the author `BEDROCK_MODEL_ID`. Existing Claude settings consequently make the author use Sonnet 4.6 adaptive/high, 16,000 shared tokens and no sampling fields. The solver/auditor already use those same settings. Source, author prompt/schema, jobs, limits and prospective criteria remain byte-identical to the baseline. This is not a matched causal experiment isolating model capability from the resulting request configuration.

Every recorded request equals the request rebuilt by the actual runtime at that stage. Nine dense reservations equal nine dispatches, below six per job and eighteen overall. Every job ends within its 240-second deadline, although the quantitative job fails its author socket timeout. Mixed ends after five calls with one remaining reservation, below the existing three-call admission floor for another mixed author pass. There is no global stop or harness replacement.

All clients use a three-second connect timeout and one SDK attempt. The last audit's read timeout shrinks to **89.786 seconds**. The unchanged factory formula implies 95,787ms remaining at configuration, versus 95,760ms captured before Converse; that 27ms interval is an inference because no separate factory timestamp was recorded. Every successful call finishes below its recorded read timeout. The timeout attempt remains explicitly failed. Offline replay establishes the same content/control flow, not a replicate of these elapsed measurements or socket-timeout values.

Reported usage from the eight available responses is **31,935 input tokens and 21,665 output tokens**. The first timed-out call's usage is unknown, so these are not complete billed totals. Provider-call elapsed time sums to **348.143470 seconds**, including the failed attempt; worker elapsed time sums to **348.867749 seconds** across independent jobs. Eight reasoning blocks were omitted. No private reasoning text or signature was inspected or inferred.

## Reproduction and limits

Python 3.12.11 imported the pinned `mixed_probe.py` and ran its actual `run_jobs` with only the SDK factory replaced by exact captured responses and the recorded error classification. Socket connections were blocked. **Twenty-seven real plan checks** verify all 44 pins before/after dispatch and completion. Exact requests, reservations, `passes`, `returned`, `returned_sources`, `returned_provenance`, call counts and statuses match. Independent JSON Schema, native adaptation and local solver/auditor parsing supplement the worker replay. A final check confirms unchanged source, plan and capture bytes.

Actual successful native cardinalities are author targets 5/2, solver counts 5/1 and audit counts 5/4/1. This does not establish provider support for every configured count. Structural success and exact compiler preservation do not prove open-domain truth, appropriate difficulty, novelty, practical distractor quality or deployment readiness. No new timeout setting, runtime patch, historical repair or criterion adjustment is used here.

Bindings:

- Plan: `6230622117349a2617e7502def86d625bd70fb401b82c3dd8f59c2a048624dcc`.
- Capture: `9402ce571dee1c386aa3489e548b82292408e9b4a984a4807d596a6ca982a707`.
- Detailed checks and replay procedure: [independent-structural-review.json](independent-structural-review.json).

Only these two derived review files were written. No source, plan, capture, gold or prior failed result was changed, and no provider or account action occurred.
