# Inactive prose task-explicitness author comparison

Preparation only: no plan has been frozen and no provider call is authorized by these files. The earlier two sentence-only English outputs remain defective. An inferred editing operation, intended antecedent, or tense restriction cannot rescue them.

Use the clean, committed constructed-quantitative author source0e4619f in the dedicated construction checkout. This keeps the actual long mixed task-only schema/instructions and native+authored_solution configuration. The new English-only scope is fixed in job-draft.json. Four independent author calls run baseline/candidate/candidate/baseline, with the same question_author_constructed_v1 schema, normalized user request, Kimi K2.5 disabled thinking,6000tokens,temperature0.2, and actual runtime request builder. The sole request change is candidate-addition-draft.txt appended to the final native system prompt. There is no solver, reviewer, bank write, repair, top-up, fallback, or reinterpretation of prior captures. Compared with historical mixed-worker jobs, this job/variety is different; there is no causal comparison to those trials.

The20requested raw slots (10per arm) are the fixed denominator. Preserve all returned rows, even invalid or extra ones; missing/badshape/failed/unattempted slots do not disappear through sanitization. A native-valid exact-five-row end_turn response within100seconds is separate structural credit. Each raw prompt must meet12–320characters, each choice1–140. Report the author's main≤320instruction separately from the immutable sanitizer's actual12–420hard bound.321–420 is an author-instruction deviation, not malformed runtime output. Sanitize raw rows only as a diagnostic using the actual preservation flag; do not score a normalized or clipped replacement as the original.

Prospective primary content measure is independently usable original rows of10per arm: a literal self-contained task with all necessary constraints, exactly one correct offered answer matching the explicit key, six task-relevant distinct choice pairs, and a true complete main explanation. Uncertainty fails this content criterion. Candidate target is10/10; report both arm numerators, all20slots, per-call variety (two agreement/two reference/one tense), raw exact-format and both length metrics separately. Candidate text/situation copying earns no usable-content credit; reviewers must explicitly check both excluded examples and the five requested new situations. Difficulty is a separate diagnostic; any actual difficulty-target regression must be reported before considering promotion. Full-worker or general semantic reliability is not qualified by this author-only repeated-scope test. No promotion follows automatically from equal performance.

Before seeing keys, explanations or treatment/call grouping, reviewers receive a projection with20opaque IDs sorted by hash, raw stem and choices only. IDs and deterministic rotations derive only from the plan hash and fixed call/question positions, never the authored key, explanation, content judgment or verdict. All20slots remain, with missing/malformed slots explicit; extras are separately flagged. A separate mapping retains call/question/arm/rotation for later unblinding. Reviewers must not open that mapping or raw capture until their literal requested operation, premises, supported choice texts, six pair judgments and uncertainty are locked. Final content/teaching review is bound to the locked worksheet and raw capture; no inferred task rescue, criterion change or repair.

Maximum4actual dispatches, one author reservation per fixed call, read100/connect3/SDKtotal1, no retries, no total wall-time cap across independent calls. Ordinary timeout/provider/safety/malformed/late-output failures give zero credit to that call's5slots and continue the remaining fixed calls. Only credentials/setup, source/request/capture integrity, or an actual call/reservation ceiling violation stops globally. All runtime/harness/job/candidate pins are checked before and after dispatch and after local assessment/completion. Request bytes must match the draft/frozen request exactly. The helper exports credentials only in memory with its15second/32768byte bound; saved captures omit provider reasoning/signatures, whitelist usage, redact safe metadata/errors and refuse visible credential echoes. The safe response retains the bounded/redacted stopReason needed for end_turn credit. Its duplicate raw runtime-metrics stopReason is not persisted.

Offline preflight and independent review must pass before root freezes the exact draft hash. Root alone owns any later authorized execution. Import, --prepare and --project do not obtain credentials or invoke a provider. --execute requires an exact frozen plan hash and refuses overwrite/resume. Reports and blind worksheets are derived artifacts; frozen source/plan/capture remain immutable.
