# Prospective two-call shared-schema solver qualification, count five only

Test the actual `SolverSlotContract(5)` v5 provider representation from `/tmp/checkpoint-solver-shared-schema/backend/bedrock-question-service`. It factors the repeated choice, pair, and solution records through internal `$defs` / `$ref` definitions. The provider schema is 1,489 characters rather than the failed v4 schema's 11,798. Recursive expansion must equal the prior logical schema exactly, including every required identity and field. The strict local validator retains the expanded contract and existing reason limits. This candidate addresses the exact-request diagnostic's compiled-grammar-size rejection; service acceptance remains unproven before this trial.

Use the same first two five-item jobs from `choice-quality-release-20260922/adaptive-plan.json` and the failed v4 structural plan. Preserve all question/input bytes, choice ordering, explicit trusted pair endpoints, model settings, case order, reviewed gold, and semantic system text. Change only the native schema representation/name and the contract-version label in its existing identity instruction. The runner verifies both request deltas and v4/v5 logical schema equivalence before dispatch. No case-specific semantic instruction or gold enters the request.

Exactly two provider calls maximum, in historical batch order, with Sonnet 4.6 adaptive thinking/high effort, no temperature, 16,000 shared tokens, region us-east-1, connect timeout 3 seconds, read timeout 75 seconds, and SDK total attempts one. No warmups, retries, replacements, rescue, resumed capture, or extra calls. Stop the entire trial after the first provider error, non-`end_turn`, elapsed time above 75 seconds, native/strict local validation failure, identity mismatch, or decoded binding/content mismatch. Preserve every failed and unattempted denominator. Semantic label or reason errors are assessed separately and do not create structural failures or replacement calls.

Structural qualification requires 2/2 bounded `end_turn` responses, both native and actual strict local validation, all 10 trusted question identities, 40 choice slots, and 60 pair slots. Every returned inner record must survive native adaptation exactly as a JSON value apart from restoring its trusted index. Local decoding must retain the exact offered text and the unchanged reason/judgment/relation fields. Existing local pair validation may reverse endpoint orientation to match the trusted offered list; both exact endpoints and their unordered binding must match. Record actual reason-before-judgment/relation output order separately.

Independently assess all 40 correctness labels, 60 pair labels, and 100 visible reason strings against the same ten reviewed controls, including false rejections and bad eligibility. Four cases are eligible and six are defective. Report label accuracy, eligibility, and reason fidelity separately from the structural criterion; no returned data means these are unassessed. These repeated diagnostics provide no population accuracy, semantic determinism, causal improvement, fresh worker yield, or production qualification claim. A service pass applies only to count five; the other supported local counts remain unqualified by this live trial. All prior failed trials retain their original outcomes.

Freeze all 23 top-level runtime Python modules, the new plan/runner/tests, dependency versions, old adaptive requests/capture/gold/review, and the failed v4 plan/capture plus diagnostic evidence. Import the frozen diagnostic's safe-error helper so future failures retain only bounded redacted AWS code/message, HTTP status, and request ID, never complete exceptions or headers. Credentials remain in memory. Preserve visible model output for strict replay and independent review, but omit provider reasoning content/signatures and retain only their block counts. Token usage, latency, truncation, and unavailable usage are reported explicitly.

Parent approval of this new plan's exact SHA256 is required before any dispatch. No source, request, prompt, schema, gold, or frozen evidence changes after approval. This proposal does not change production defaults, policy, model settings, or deployment.
