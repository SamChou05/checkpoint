# Native downstream formatting regression evidence

See the [prospective protocol](../../QUESTION_NATIVE_STAGE_PROBE_PROTOCOL.md)
and [results](../../QUESTION_NATIVE_STAGE_PROBE_RESULTS.md). Ten local model
requests completed; all native schemas/adapters and the stated stage checks
passed. The actual review-policy replay nevertheless admits the defective
spreadsheet item. These records do not qualify fresh generation or deployment.

## Files and provenance

- `plan.json`, `capture.json`: exact committed-source plan and final-response
  capture, including every planned slot, actual dispatch, usage and worker
  lifecycle. The original run handle was 10264 and exited zero; do not restart
  or resume it. The raw response precedes any reviewer feedback adaptation.
- `profile-preflight.json`, `sdk-preflight.json`, `plan-audit.json`: read-only
  AWS profile resolution, local SDK request checks, and independent binding
  audit. Account IDs, credentials, provider headers and exception text are
  excluded. A profile resolution is not production-role IAM qualification.
- `operational-audit.json`: independent verification of the completed capture,
  every derived summary row, all 66 frozen sources, and exact reproduction of
  the four actual-policy outcomes with provider entrypoints forbidden.
- `subjects.json`, `subject-mapping.json`, `independent-subjects.json`,
  `first-pass-freeze.json`: all 19 original subjects, correspondence to seven
  historical calls, and the independent first pass frozen before live requests.
  The assessor saw only `subjects.json`. Neither mapping nor model outputs was
  disclosed during that pass. The assessor is fallible, not expert-calibrated.
- `summary.json`, `analyze-capture.py`: exhaustive stage/usage counts and
  subject-level comparisons, including all repeated occurrences. Eight
  disagreement labels are preserved; three reflect the substantive
  cannot-determine distinction explained in the results, not demonstrated
  model errors.
- `policy-replay.json`, `replay-policy.py`: actual application verification
  with exact historical author/solver evidence and fresh native reviewer
  responses. Eleven requests through the two target reviewer calls and the
  solver/reviewer subject strings match their archives. No provider or storage
  calls occur. Root reran
  the helper and reproduced the complete report bytes exactly.
- `integration-tests.txt`, `probe-tests.txt`: 1,046 integrated backend tests,
  then 59 targeted tests including 14 new probe tests, on the pinned runtime.
  Expected CLI-error output is generated by rejection-path tests; both test
  runs end successfully.

The helper scripts are archived exactly as executed and contain their original
local capture/checkout paths. To reproduce elsewhere, remap those paths in a
separate copy; do not overwrite original captures or present a remapped script
as the original hashed helper. The production/evaluator source is bound to
commit `751bdaa3405d1c358103af51093018364365947e` and all 66 recorded source
hashes. Later documentation-only commits do not alter that frozen runtime.

`manifest.json` binds every other file in this directory by bytes. The directory
contains synthetic question/source context and final model records; hidden
reasoning content and credentials were never retained by the observer. No
question in the policy audit was written to a learner's inventory.
