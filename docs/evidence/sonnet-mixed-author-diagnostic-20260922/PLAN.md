# Draft Sonnet mixed-author diagnostic

Unfrozen preparation only. No inference, account agreement, deployment or production
change is authorized. Parent must review the final draft before a separate freeze
and authorize its exact hash before dispatch. Earlier failures stay unchanged.

Use the three exact original normalized requests from the frozen quantitative
mixed-route plan: quantitative, Python, then mixed arithmetic/English, five items
each. Preserve request data, assignment allocations, goal/difficulty/novelty scope,
order, native mixed-author prompt and schema. There are fifteen requested slots,
including missing or failed rows. No new problem-specific corrective hint is added.

Change only the author configuration to `us.anthropic.claude-sonnet-4-6`, adaptive
thinking/high effort, 16,000 combined output tokens and no sampling parameters.
This is a model-and-reasoning configuration candidate, not a modelId-only or
concurrent causal comparison with Kimi. Reusing prior job specifications does
not create a new held-out domain or erase the original pipeline failures.

Make exactly one eligible author dispatch per independent job, at most three
provider calls total. Invoke the actual frozen `_generate_provider_payload` with
`ProviderCallBudget(1)` and no fallback; the real `_bedrock_client` config must
have read 100 seconds, connect 3 seconds, one SDK attempt, us-east-1 and the fixed
Bedrock runtime endpoint. Never call the solver, reviewer, repair, top-up, account
activation or bank APIs. Check exact request equality before dispatch. No retries,
warmups, replacements, rescues or resume. A normally completed call above 100
seconds is separately reported and fails the prospective elapsed criterion.

Ordinary timeout/provider/output failures consume that job's only attempt, earn
no successful-batch credit, and do not prevent the next unchanged independent
job. Credentials/setup/SDK/native-configuration failures, source/request drift,
unsafe capture or budget-integrity failures stop globally. All fifteen requested
slots remain visible, including globally unattempted jobs. Usage missing after a
timeout is unavailable, never zero. Capture exact visible output and bounded safe
AWS error fields; omit all provider reasoning text and signatures, retaining only
block counts. Runtime receives the original response unchanged.

Before any semantic assessment, capture the unmodified author output with source
identity `[job_id, original_row_position]`. Validate the actual native envelope.
Compile every quantitative row independently using the unchanged flat adapter
and exact rational compiler. Preserve compiler failures and every original row;
never fall through to prose, repair a spec or replace a proposed key. Record
exact compiler-derived five-field content for valid typed rows and untouched
prose draft fields for ordinary rows. No candidate receives a verification stamp.
These local checks are pre-sanitization observations, not runtime admission.
Runtime length diagnostics count raw Unicode characters without clipping: prompt
12–320, choices 1–140, main explanation 12–420 and compiler choice feedback
12–280. The unchanged prose author instruction separately requests a main of at
most 320 characters. Record compliance with that instruction independently:
321–420 characters is an instruction deviation, not a runtime length failure.
Primary usable-item assessment uses the runtime maximum of 420; an instruction
deviation alone does not fail usability. Missing compiled content is unavailable,
not length-valid.
Allocation diagnostics resolve supplied metadata through the unchanged local
assignment matcher and count every raw row, including failed specs. They do not
establish actual topic fit, difficulty or semantic usability; those need the
independent review. The author's difficulty is recorded only as its claim.

Prospective diagnostic requirements (not production qualification):

- Three normal `end_turn`, native-valid responses inside the fixed bounds, each
  with exactly five rows; extra rows are preserved but cannot replace missing
  requested slots or earn primary credit.
- At least fourteen of fifteen original slots independently usable as authored,
  at least four per job. All counted items must have a unique supported key,
  four meaningfully distinct and plausible answers, correct complete teaching,
  requested assignment/topic fit, sufficient premises and difficulty at least 2.
  Uncertainty fails the item; local formatting success is not semantic credit.
- At least four compiled quantitative items in the quantitative job and two in
  the mixed job; at least one correctly assigned English prose item in the mixed
  job. Python must test actual Python semantics, not substitute arithmetic.
- Independent assessment covers all raw drafts, including local rejections,
  authored mains, all generated compiler teaching and unsupported generalizations.
  Assessors first solve prose stems/choices without author keys/main, then inspect
  the teaching. Record raw count/length/allocation failures, compiler rejection
  types, exact content identities, tokens and latency independently.

A favorable result would support testing this author configuration in the actual
three-stage worker. It cannot qualify the solver, reviewer, overall bank yield,
240-second worker deadline, topic-wide accuracy, defaults or deployment. A slower
but better author can leave too little time for verification; measure that tradeoff
before selecting a runtime configuration. No comparison percentage is a population
accuracy estimate.
