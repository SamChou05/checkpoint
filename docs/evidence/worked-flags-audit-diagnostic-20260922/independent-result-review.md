# Independent result review — worked mains and native flags

**This bounded component diagnostic passes:** two of two calls are native/local valid; all five sound worked explanations are accepted, and all five material falsehood copies are rejected. All ten exact keys, ten main-support labels, ten validity labels and 60 boolean flags match the prospectively approved controls. Only the explanation flag is true on each negative control; all flags are false on the positives.

I inspected every returned row, replayed all 27 source and 8 artifact pins, checked both actual requests against the frozen plan, and reran native adaptation, local validation and scoring with network sockets disabled. The five controlled main strings are the only subject-data difference between the two requests. Explicit keys, gold and synthetic solver records are not supplied to the auditor; the main can disclose the intended key as it does in the real route.

Independent Fraction arithmetic confirms the four exact results 7/12, 3/2, 47/20 and 3/2. Enumerating integers 1–20 makes 14 the minimum for 3x>40. The five negative explanations assert false subtraction, product denominator, fraction conversion, extremum-boundary evidence or reciprocal equality. In particular, permitted 15 gives 45>40 and directly disproves the changed all-larger-fail claim. The auditor's flags agree with these gold classifications; **no rationale was returned**, so the flags do not establish which counterexample or computation the model used.

Call times are 39.762440s and 34.432756s, under the 100s bound with 100s reads, 3s connects and SDK attempt count 1. Usage is 5,754 input and 5,340 output tokens, with no missing call usage. The minimum task receives difficulty 3 in the sound batch and 2 in the false-main batch despite unchanged stem/choices; both meet the requested minimum 2 and requested 2–3 range. This bounded rating variation does not alter the correct key or material main-support result.

Actual HTTP200 end-turn responses establish **native count-five schema acceptance** for `authored_solution_reviewer_v3_n5` on the tested Sonnet 4.6 adaptive/high configuration. They do not establish acceptance at every count 1–40. The result concerns ten familiar component controls, not full-worker yield, arbitrary topics, an error-rate estimate or deployment readiness. Both renderer and transport representation changed from the prior failed trial, so this does not isolate a causal effect. Negative copies remain auditor-only controls, never approved compiled items, and no learner content was released.

The frozen plan/capture/gold and source bytes remain unchanged. Their exact SHA bindings and every reviewed row are in `independent-result-review.json`. The capture's provisional `qualified:false` and pending-review strings remain untouched; this separate derived review records the completed component assessment.
