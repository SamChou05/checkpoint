# Worked explanations and native issue flags — draft only

No provider dispatch or freeze is authorized. This is a two-call component diagnostic of the current immutable-main auditor, not a worker or production qualification. Runtime is isolated at `/Users/samchou/.codex/worktrees/worked-flags-audit-source/Checkpoint`, commit `61db5a5a226009bba848706ad01a78c939f2459b`; future root integration changes cannot alter that checkout's source pins.

The first batch contains the five initial quantitative specs from the unchanged failed immutable-main mixed capture. The new compiler renders four worked arithmetic mains and preserves the explicit-minimum main. Exact stems, choices, keys and all four compiler feedback strings otherwise match the original payloads. The second batch copies those five tasks and changes exactly one span of each main into an unambiguous material falsehood. No question, option or key is repaired or corrupted. Each bad explanation still names the original correct answer; agreement with that answer is insufficient to approve the teaching.

| Source index | Sound reasoning | Deliberate false claim and counterexample |
| --- | --- | --- |
| 0 | 9/12 minus 2/12 is 7/12 | The changed intermediate result says 8/12; subtraction gives 7/12. |
| 1 | Product 18/12 reduces to 3/2 | Changed denominator 10 makes 18/10 equal 9/5, not 3/2. |
| 2 | 7/4 converts to 35/20 | Changed conversion 34/20 is not 7/4; adding 12/20 gives 46/20, not 47/20. |
| 3 | Minimum x is 14; all smaller permitted integers fail 3x > 40 | Changed proof says all larger values fail, but the offered and permitted 15 gives 45 > 40. |
| 4 | Dividing by 5/9 multiplies by 9/5 | Changed reciprocal step multiplies by 5/9, producing 25/54, not 3/2. |

Every main is 82–154 characters, within the unchanged 420-character runtime bound. The false copies are evaluator-authored audit controls, **never releasable compiled items**. The packet records the genuine recompiled five-field learner payload separately from each audit control. It must never manufacture a private `CompiledCandidate`, elevate a control to policy 6, or treat an auditor acceptance as compiler provenance.

## Exact proposed route and limits

Call 0 audits the five sound mains in original source order. Call 1 audits the five false mains in the same order. Both receive the same historical quantitative goal, topics, absent assignment/source context and empty history. Provider item identities are ordinary dense indexes 0–4; local `sound`/`false_main` labels, gold, provenance, specs, explicit keys and author difficulty are withheld. As in the real route, the main can imply the intended key and that claim is audited.

Use the unchanged actual `question_verification.verify_questions` authored-solution input builder and its system prompt, with the current `AuthoredSolutionFlagReviewContract(5)` native wrapper and serializer (`authored_solution_reviewer_v3_n5`). Build the audit request with an explicitly evaluator-authored complete-choice fixture solely to advance the local input builder through the existing solver seam. This is not an independent model solver judgment, costs no provider call and is not sent to the auditor. The fixture labels come from the closed mathematical tasks, whose choices and unique key are identical across batches. Stop request construction at the review callback; it does not approve or release items. The actual audit view includes the main and omits all four choice explanations, exactly as the production main-only route does. No alternative prompt, extra explanation request, free `issues`, rationale field or repair stage is added.

Obtain exact provider request objects through the actual `_generate_with_bedrock` request builder with an injected recorder. Only the two frozen review requests will later be dispatched. Model is `us.anthropic.claude-sonnet-4-6`, adaptive thinking, effort high, maxTokens 16000, no temperature or top-p. Each real SDK client uses read timeout 100 seconds, connect timeout 3 seconds and total attempts 1. No retry, warmup, top-up, replacement, solver, author, bank write or deployment. Maximum two dispatches and ten item decisions; no worker deadline or full-pipeline throughput claim is made.

Run both independent planned calls after ordinary provider timeout/error, non-end-turn, malformed model output or a semantic miss. A failed batch receives no structural or semantic credit and is never retried. Stop globally only for credential/setup failure, source/request/config drift, exhausted call limit, or capture integrity failure; preserve every unattempted case in the original denominator. Record actual elapsed time and token usage, distinguishing absent usage from zero. Enforce the existing output/capture bounds and strip provider reasoning text/signatures, retaining only omission counts and safe bounded errors.

## Prospective criteria for root review

Structural success requires 2/2 native end-turn responses, exact closed five-ID coverage, boolean issue flags with all six required categories, native adaptation and the unchanged strict local review parser. Report each call's elapsed time against the 100-second criterion separately from any socket timeout. A malformed whole batch earns zero item credit; no selective repair or salvage.

The diagnostic succeeds only if all five sound items are accepted with supported mains, no true flags, the exact independently known answer and an admissible difficulty of at least 2; and all five materially false mains are rejected with unsupported main support and the explanation flag set. Require exact answers for all ten unchanged tasks and independently assess all flag/disposition consistency, even for a rejected item; a wrong key does not become correct merely because another veto prevented release. False positives, false rejections, uncertainty and structural failures retain their full denominator. The harness reports only provisional mechanical agreement; `qualified` remains false pending root’s independent output assessment. No returned rationale is expected or scored, because the chosen contract contains no free rationale. The observed flags cannot prove the model caught a particular false equality, so the report must not infer an unreturned explanation.

This compares current behavior with a preserved prior failure, not a paired causal intervention: the worked renderer and native flag contract both changed. Familiar specs, deliberately clear bad copies, easy arithmetic and matched source order limit generalization. A clean result would demonstrate these ten stage-level outcomes and native grammar acceptance at count five; it would not establish full-worker yield, broad topic reliability, universal semantic certainty or deployment readiness. An independent reviewer must approve every sound/corrupt main and gold before freezing exact requests, sources, controls, harness, budget and plan.

## Minimal offline harness checks to add after control review

Reuse the bounded capture helper and existing recorder/native replay patterns. Hash all runtime files before importing them; pin requirements and root’s approved gold review. Persist each exact request/call reservation before dispatch, and check exclusive capture ownership before every update. Any SDK client-factory failure stops globally. Recheck pins after scoring and at completion before credit. Verify the exact current prompt/config/schema, hidden keys and gold, source-only rotation and five original identities, unchanged task/choice bytes between batches, all ten controlled main lengths, compiler rejection of every corrupted five-field payload, and native/local flag decoding. Fake two independent calls must preserve failures and ten-item denominators, continue after an ordinary timeout/structural failure, stop on sticky source/setup/capture drift, forbid resume or a third dispatch, and retain no credentials or reasoning content. No full new framework is needed.

Draft packet SHA256: `3463294975e19e8f1f71a562a77a4f79c57bbea712418ce6eba569a39b97795f`.
