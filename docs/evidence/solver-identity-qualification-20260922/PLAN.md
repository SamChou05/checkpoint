# Prospective two-call solver identity qualification

This is a structural qualification of the count-bound solver envelope. Copy the first two unchanged count-five jobs from `choice-quality-release-20260922/adaptive-plan.json`. Preserve their exact input strings, order, model, adaptive-high settings and all ten reviewed gold cases. Change only outputConfig to the actual `SolverSlotContract(5)` configuration and append its native identity override to the unchanged solver system text. The existing inner choice/pair schemas, semantic instructions and endpoint bindings remain unchanged. No production semantic prompt, policy, model or thinking default changes.

Exactly two calls are permitted: Sonnet4.6, adaptive high, no temperature,16000 shared tokens, region us-east-1, SDK total attempts1, connect3s/read75s. No warmups, retries, replacements, repairs or additional calls. Exclusive plan/capture files prevent overwrite or resume. Stop on the first provider failure, non-end_turn, native/strict local validation failure, identity/endpoint/content mismatch or provider elapsed time above75s. Keep all failed and unattempted cases in the denominator. Semantic errors are recorded without stopping the remaining planned call or being relabeled.

The structural pass criterion is2/2 bounded end_turn responses, valid native and actual local solver validation, exactly10 trusted identities,40 choice judgments and60 pair judgments, and exact unchanged bindings/content through the adapter. Each raw closed map must have exactly0–4 keys without a nested index. Restore only those trusted indexes; compare the resulting inner records byte-for-byte as JSON values with the received records. Decode exact choice and pair texts through the actual solver using the same frozen input. All reason bounds remain the existing local solver bounds. No semantic result is needed to establish shape and identity correctness, and no structural result establishes semantic correctness.

Independently score every returned supported/refuted/uncertain choice declaration, equivalent/distinct/uncertain pair declaration, reason and resulting eligibility against the unchanged reviewed gold. Report these separately from structural qualification, including any wrong rationale with a correct label. All ten controls are repeated diagnostics, not new holdouts. The historical four-call adaptive trial and every later failed experiment remain immutable. No broad accuracy, deterministic meaning, causal improvement or worker-yield claim follows from this two-call envelope test.

The runner imports the actual isolated runtime at `/tmp/checkpoint-solver-identity-integration/backend/bedrock-question-service` and pins every top-level Python source file, dependency versions, new runner/tests/plan document, source adaptive plan/capture and original gold packet/review. Keep that checkout unchanged through qualification. Replay requires these pinned runtime bytes; a later production checkout is usable only if all bound source hashes match. Gold and explicit author keys never enter provider input. Credentials remain in memory; provider reasoning blocks/signatures are omitted from capture, with counts only. Parent approval of the final exact plan hash is required before any dispatch.
