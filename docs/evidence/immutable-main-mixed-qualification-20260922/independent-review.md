# Independent learner review — immutable main mixed route

The four returned Python questions have unique supported keys, meaningfully distinct exact-result choices, and supported mains in item context. All four original author mains are byte-preserved and safe under all 24 display orders per item. The trial nevertheless fails: **4/15 returned**, with 0/5 quantitative, 4/5 Python and 0/5 mixed, below the frozen 14/15 and 4/job thresholds. No compiled or English item reaches the learner.

Blinded solving was locked before keys, teaching and model judgments were opened. The four exact answers are `True`, `40`, `"odd"`, and `['b', 'c']`. All fit the Python scope and requested minimum difficulty 2. The reviewer rated the first item 3; the independent rating is 2, still admissible. There is no supplied history from which to establish novelty beyond these distinct returned tasks.

The first main correctly groups and simplifies `True or (False and False)`. Its word “then” could sound like an execution trace, although actual Python short-circuits at the first `True` and never evaluates the right subtree. I assess it as a supported compositional simplification of this closed expression, with that wording caveat, rather than a false claim about actual side-effect evaluation. This contextual standard is consistent with the earlier operand-return adjudication. Index, conditional and slice teaching are supported. Quoted input letters and list positions are data, not shuffled answer labels. [Python expression reference](https://docs.python.org/3/reference/expressions.html)

## What was rejected or unavailable

The first five quantitative drafts compile to correct keys. Four exact-value mains only state the result; the auditor reasonably asks for common-denominator, multiply/reduce or reciprocal steps, and its proposed numerical steps are correct. The fifth, explicit minimum item receives a positive verdict but malformed issues `['', ' ', ' ']`. **Actual local strict replay rejects the entire review batch**, so the zero result must not be attributed solely to four pedagogical vetoes. No repair or partial rescue was applied.

The five quantitative top-up drafts are also mathematically correct (31/24, 3/2, 11/10, 6 and the explicit maximum 12). Their solver times out before any result. The rejected Python draft `0 or 'hello'` has unique answer `'hello'`; its solver wrongly equates wrong choices `0` and `False` because they compare numerically equal. Exact returned integer and Boolean results remain distinguishable in this task. This is a model pair-classification false rejection, not a released defect. [Python built-in types](https://docs.python.org/3/library/stdtypes.html)

## Operational denominator

There are 10/18 permitted calls: 5 quantitative, 4 Python, 1 mixed. Seven return native end-turn responses; three time out. Job elapsed times are 234.210s, 226.355s and 100.261s, all within 240s. The late quantitative solver receives a 2.075s socket timeout from the remaining-time budget. Reported usage is 29,146 input and 32,260 output tokens; three calls have unavailable usage, which is not zero. The Python top-up and mixed initial author yield no text to adjudicate.

The accompanying JSON binds the frozen plan, capture and locked blind reviews, records exact item/source/main hashes, local malformed-review replay, 96 display permutations, and every call’s timeout/budget. No provider calls or frozen artifact changes were made. All 15 requested slots remain in the denominator. This is a failed combined pipeline qualification despite sound admitted content; it is neither a release endorsement nor a universal semantic guarantee.
