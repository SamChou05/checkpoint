# Draft combined verifier diagnostic

This is preparation only. No provider call or plan freeze is authorized by this file.

Use the unchanged 24 full learner responses and original 12 sound / 12 defective gold in `final-content-audit-20260922/adversarial-plan.json`. Preserve original source order while regrouping into **5 + 5 + 5 + 5 + 4**, the worker's maximum of five. All four original requests have identical goal, empty skill map and empty source documents; the builder checks exact equality before regrouping. Canonical scope SHA256: `6d5f44c5e05aed873a638d7bed3a84e19a88aad2f77e191e21d5e83f1a3ca8c8`. The solver requires absent skill maps as `null`, so this new trial prospectively changes only the empty `skillMap: []` representation to `skillMap: null` in both stages. The draft pins both exact scopes and hashes; the adapter rejects nonempty skill-map lists and copies all other data exactly. Regrouping changes companion context and is not a causal comparison with earlier batches. The known controls are not fresh.

Treat each original as frozen authored output; make no author model call. Invoke the actual isolated runtime's `verify_authored_feedback`, production provider callback, native solver v5 / immutable audit v3 adapters, client factory and `ProviderCallBudget`. The blind solver sees every original in a batch. Its actual vetoes determine dense audit survivors; zero survivors skip the audit. Audit inputs retain exact original teaching and scope with empty assignment descriptors/history, hiding the explicit key and authored difficulty. No output is repaired or learner text rewritten.

Use Sonnet 4.6, adaptive thinking / high effort, 16,000 maximum output tokens and no temperature in the request. Each independent batch has a fresh fixed 240-second context, maximum two stage calls, connection timeout 3 seconds, actual production read timeout shrinking from a 100-second ceiling, and SDK total attempts 1. Maximum five batches / ten calls; no retry, replacement, top-up or capture resume. Ordinary provider, model-format, non-end-turn or deadline failure ends that batch; continue the other fixed batches. Credentials/access/setup or source/configuration/binding/content/budget integrity failure stops the entire run. Recheck every frozen runtime, harness and source-artifact pin before and after each actual dispatch, as well as between batches; a mid-call change earns no batch credit. Record the complete planned denominator even if failures prevent output.

Primary criteria are five normally completed batches, all 24 original admission decisions correct, all 12 sound originals retained, all 12 defective originals excluded, exact selected learner fields, valid native/local stages ending in `end_turn`, and actual transport/batch deadlines met. A failed batch earns no semantic credit, including for defective items it did not deliver. Difficulty 1–5 is allowed through minimumDifficulty 1 and no adaptive targets; difficulty is diagnostic, so this is not worker qualification.

Report every available choice/pair/task/answer/feedback judgment and short reason separately, using the approved field-expectation draft without changing original gold. An item rejected by the solver has no audit assessment; missing fields do not earn correct-label credit. Independent review is required for reason fidelity and observed failure mechanisms. This test cannot rescue any prior failed standalone trial, qualify fresh generation or yield, or establish universal semantic correctness.

The generated draft references original payload and whole-gold hashes without copying the full gold. It pins every runtime Python source, the harness/tests/protocol, both source artifacts, exact configurations and count-specific contracts. Capture records actual requests, response JSON text, safe bounded provider error details, available usage, short judgments, actual transport settings and remaining time. Provider reasoning text/signatures, credentials and raw headers are excluded. There are no question-bank writes or deployment actions.

Preparation: run `python -m unittest discover -s docs/evidence/authored-feedback-combined-controls-20260922 -p 'test_*.py'`, then `python .../combined_probe.py --draft`. Parent review and explicit hash-bound authorization are required before the separate `--freeze` and `--execute` operations. No such operations have been performed during preparation.
