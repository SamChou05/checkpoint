# Scope-aware immutable-feedback component qualification: draft

Test the actual corrected audit candidate at `/tmp/checkpoint-authored-feedback-scope/backend/bedrock-question-service` on the same 24 known learner payloads, original admission gold, batch order, choice order, and goal/source scope. The actual new system prompt includes assigned skill/objective fit and novelty checks plus the existing difficulty rubric. The actual builder supplies its new sanitized assignment/history context. These known controls have no assignment metadata or prior question history, so those descriptors and history are empty. A pass cannot establish model behavior on nonempty assignments or history; separate offline integration tests cover their transport and validation rules.

This is a joint context/input/prompt and timeout candidate, not a latency-only or controlled causal experiment. The original v2 four-call qualification remains failed at 0/4 after its first 75.167173-second read timeout. The subsequent latency-only v2 draft remains inactive. Neither earlier artifact is replaced, amended, or rescued.

Exactly four sequential provider calls, six known controls each, are proposed. All 24 required decisions remain 12 accept and 12 reject. Use the final actual scoped runtime prompt and native `AuthoredFeedbackReviewContract(6)`, with its unchanged task/answer/difficulty and five-feedback assessment shape. Every learner string remains exact; the reviewer must not write replacement learner text. Provider input hides explicit author keys and difficulty, gold, expected labels, and previous approvals, while authored teaching may reveal the intended answer. No new semantic guidance or control-specific example is added by this harness.

Sonnet 4.6 in us-east-1 remains adaptive/high with 16,000 shared tokens, no temperature, connect timeout 3 seconds, SDK total attempts one, and a prospective read/elapsed bound of 100 seconds. The existing runtime ceiling is 100 seconds. Its actual client factory still shrinks socket timeouts to fit remaining invocation time; no worker deadline or production setting is changed here. A later fresh full-worker qualification must measure the real factory under the unchanged 240-second worker deadline and six-call ceiling.

Primary criteria are four strict native/local `end_turn` responses within 100 seconds each, all 24 frozen admission decisions correct, all 12 sound controls retained and all 12 defective controls rejected, and every selected learner payload equal to its exact original. Difficulty retains the same explicitly permissive test predicate and is reported independently; it cannot excuse an admission error. All prior interpretation-dependent annotation caveats remain explicit and do not alter admission gold.

Independently assess all 24 task labels, 24 answer declarations, 120 feedback labels, and 144 visible task/feedback reasons. Report admission, individual labels, reason grounding, difficulty, and schema/transport separately. Private reason errors remain findings even when admission is correct. These exposed, repeated diagnostic controls include paired variants; this is not fresh semantic or worker qualification, author/solver qualification, a population accuracy estimate, or a deterministic-quality guarantee.

Attempt all four independent, preplanned batches once. An ordinary provider timeout/error, non-`end_turn`, elapsed time above 100 seconds, or malformed native/local output fails that batch and earns zero credit; continue only to the next unchanged planned input. Keep each failure on its own call record, without carrying a stop reason into the next batch. Stop globally for source/request/hash drift, credentials or SDK setup failures, or budget/content-integrity failures. Check pinned sources and plan integrity before and after every call. Preserve all four calls, 24 items, and 144 reasons in the denominator. This methodology was changed prospectively before freezing; all passing criteria still require 4/4 and 24/24. No earlier hard-stop trial changes its outcome. Semantic errors also do not stop remaining planned calls. No retries, warmups, replacements, repaired responses, rescue, resumed capture, or extra calls are allowed. The 100-second bound is the final timeout adjustment; there is no open-ended timeout search.

Capture exact provider requests, returned JSON text, safe usage fields, stop reasons, timing, strict validation outcomes, each assessed field, and exact selected-original hashes/content. Omit provider `reasoningContent` text and signatures, retaining only block counts. Never save credentials, headers, complete exceptions, or arbitrary provider metadata. Error details are bounded and credential-redacted. Missing usage is unavailable, not zero. The visible short audit reasons remain evidence for independent review and are distinct from omitted provider reasoning blocks.

The plan pins every runtime Python module, dependency version, harness file, and original source. Complete original gold/provenance stays in the already-frozen source files bound by hashes. The new plan keeps exact learner payloads and a projection of unchanged admission decisions and field labels/proofs used by the scorer, avoiding repeated copies of the full historical gold. Every original gold object is verified unchanged before that projection. Full provider requests remain embedded and individually hashed for review.

Offline tests use only fake clients. They retain the existing four-batch oracle, exact string/selection, semantic-versus-structural separation, malformed batch, redaction, source drift, bounded attempts, elapsed boundary, and complete-denominator checks. They verify continuing unchanged batches after different ordinary failures, no failure-state carryover, and global stops with preserved unattempted denominators for setup, credentials, or integrity failures. They also verify the actual runtime clamps 101 seconds to 100, shrinks its read timeout with less remaining invocation time, rejects near-deadline calls, and refuses a seventh call.

The scoped runtime source-ready signal has arrived after 1,223 backend tests and 54 focused tests passed. This file and the generated draft remain unapproved for dispatch. Root must review the final draft, actual request deltas, compact-gold projections, and independent-batch failure handling before freezing. Root must separately approve both freezing the reviewed draft and dispatching the final frozen SHA256. No source, prompt, schema, control, gold, or request changes may be added after authorization. No cold-versus-warm compilation or unique latency-cause claim follows from repeating requests at a later time.
