# Prospective general counterexample-auditor trial

This separate candidate uses the parent-approved generic adversarial prompt to try valid interpretations of every non-key alternative and seek counterexamples to claims of necessity. The prompt contains no specific grammar, punctuation, numerical or historical-case examples. Its audit reasons must be grounded in supplied content and justified facts without speculation about unseen learner actions or content production. Earlier prompt, plans, captures, gold and failed outcomes remain immutable. No further prompt changes are permitted after parent approval without renewed review.

Four Sonnet4.6 adaptive-HIGH calls are planned, each with six items. The first three preserve the exact original eighteen payloads and within-batch ordering from adaptive-only-plan.json; only the system text changes. The fourth batch adds six independently designed full learner responses, three sound and three defective, with different situations rather than paired repairs. The designer does not read the candidate prompt before completing that packet. Independent literal-gold review and parent review are required before this plan is frozen. Gold, case labels, provenance and repair relationships never enter model input.

The same closed count-six native output schema, 600-codepoint nonblank reason bound and immutable-content selector apply. Same us.anthropic.claude-sonnet-4-6 model, adaptive high, no temperature, 16000 shared output/reasoning tokens, read75s/connect3s, region us-east-1 and SDK total attempts1. No temperature or effort comparison is introduced. Existing native schema hashes remain unchanged. The first three exact provider requests differ solely in system text; the fourth differs in its new learner items while keeping scope/model/prompt/schema/settings identical.

Qualification requires24/24 exact dispositions,12/12 sound items accepted,12/12 defective items rejected, every accepted payload unchanged, and all audit reasons factually grounded and no longer than600 codepoints. Uncertainty or a material false/unsupported reason fails. Complete native and strict local checks plus end_turn are required for all four calls. All eighteen old cases and six new cases retain full denominator, including failed or unattempted batches. Independent manual audit covers every returned reason.

Maximum four dispatches. No retries, warmups, substitutions, repairs, replacements or extra calls. The first provider failure, non-end_turn response, native format failure or local bound/identity/type failure stops the trial; semantic misses remain recorded without triggering replacements. The wrapper requires exactly four planned entries and a fresh empty capture before invoking the original no-retry loop. Exclusive new plan/capture files prevent overwrite/resume of earlier trials. Provider reasoningContent text/signatures are omitted from evidence; only counts remain. Credentials are held in memory only.

This is a new bounded candidate qualification, not a relabeling of earlier model failures or a contemporaneous randomized causal estimate. The original eighteen cases remain repeated diagnostics; the six independent situations add prospective coverage without proving broad-domain accuracy. A component pass would still require a fresh full-worker content/yield test within explicit call/deadline budgets. No production prompt, routing, model default, earlier-stage behavior or deployment changes are authorized here. Parent must review the completed new gold and approve the exact frozen plan hash before dispatch.
