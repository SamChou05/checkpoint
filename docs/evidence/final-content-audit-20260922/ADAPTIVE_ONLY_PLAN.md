# Separate prospective adaptive-only qualification

The preceding six-call comparison remains failed and immutable. It stopped after four dispatches when the disabled configuration returned a 748-character reason against the unchanged 600-character cap. The parent requested a separate, explicitly bounded qualification of the adaptive final auditor only. This is a new plan/capture, not a continuation or replacement of the failed comparison.

Exactly three calls copy the prior plan's complete adaptive request objects at original sequences1,2,5. The first two six-item batches are repeated diagnostics already seen by that configuration. The third supplemental batch was previously unattempted in both configurations. The same eighteen learner payloads, gold labels, case ordering, system prompt, native count-six schema and settings are retained byte-for-byte. No new controls, repaired requests, changed criteria, baseline calls or prompt adjustments are introduced.

Each call uses Sonnet4.6 adaptive high, no temperature, 16000 shared output/reasoning tokens, Converse native output, us-east-1, read75s/connect3s and SDK total attempts1. Credentials remain in memory. At most three dispatches; stop all later calls after the first provider, non-end_turn, native shape or strict local format failure. No warmups, retries, replacements, repeated dispatches or rescue. A semantic miss remains a miss and does not trigger replacement. All failed or unattempted items stay in the18-item denominator.

Qualification requires18/18 exact dispositions: accept all9 sound payloads, reject all9 defective payloads, preserve all accepted original content byte-for-byte, and provide factually sound item-specific reasons no longer than600 codepoints. Uncertainty in disposition or material reason accuracy fails. Every response must satisfy strict closed identity/type/field validation and finish normally. Provider reasoningContent/signatures are stripped from evidence; only block counts remain. All returned reasons receive independent manual review. No after-output gold changes are allowed.

The wrapper imports the unchanged original offline runner's source-independent contract, scorer and redaction helper. It separately enforces a three-call ceiling, creates exclusive adaptive-only plan/capture files and pins its own source/tests/protocol plus the original plan and failed capture. The original runner, tests, prompt, schema, packets, reviews, plan and capture remain unchanged.

A pass would be a narrow diagnostic component result, not a causal estimate, population accuracy rate, broad release qualification or permission to change production. It would apply only to a final read-only auditor after frozen prose; it would not qualify the earlier globally adaptive full pipeline, which failed its worker test. A fourth stage still requires explicit call/deadline logic and fresh end-to-end yield/content qualification. Execution needs parent approval of this new plan's exact hash.
