# Parent-approved changes before freeze or dispatch

PROPOSAL.md preserves the original four-call design. Before any plan.json was frozen and before any provider call, the parent expanded the experiment to six calls and clarified the diagnostic reason field. This is a prospective scope amendment, not a retry or a change after observing output.

The first approved twelve-case packet and its independent review remain byte-identical. A separate supplemental-controls-draft.json adds six payloads as three sound/defective pairs: the known semicolon/however ambiguity with an explicitly qualified sound task, semantically equivalent wrong distractors with a distinct repair, and a representation-sensitive written-format task contrasted with a value question. This addresses the independently identified lack of duplicate-choice negatives and a language ambiguity in the original packet. Each supplemental payload receives independent gold review before freezing.

The comparison now has three six-item batches and eighteen items per arm. Call order is batch0 disabled→adaptive, batch1 adaptive→disabled, batch2 disabled→adaptive. All earlier model, sampling, output-token, native schema, latency, retry and stopping bounds remain unchanged; total dispatch ceiling becomes six. Each arm must have18/18 exact dispositions, retain9/9 sound payloads, reject9/9 defective payloads, preserve accepted content and provide sound audit reasons. Uncertainty still fails. No post-output label changes, extra calls or rescue are allowed.

The parent also replaced one sentence in the audit-only prompt before freezing. Original: “Do not rewrite, repair, supply a competing answer, or return any learner content.” Approved replacement: “Your reason may name counterexamples or corrected calculations to explain a finding, but do not rewrite or return a replacement question, key, or feedback.” The following exact-original-content sentence remains unchanged. This permits brief factual evidence for a rejection while the closed reason/verdict schema still forbids replacement learner fields. The change does not ask the auditor to write learner feedback.
