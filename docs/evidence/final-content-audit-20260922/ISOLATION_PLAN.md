# Prospective six-call singleton diagnostic

Exactly six calls each receive one of the six unchanged supplemental learner payloads from the original Sonnet adaptive-only plan. Their sequence is unchanged: defective numeric duplicate, defective semicolon, sound numeric choices, sound decimal representation, sound semicolon, defective unrestricted equal-value question. All six are reused diagnostics. Keep the original short auditor prompt, task scope, model and settings. Copy each exact learner payload into trusted ID `0`; replace the count-six output configuration only with the same prototype's count-one native contract. The provider never receives gold, case labels, provenance or the other five items.

The source plan is `adaptive-only-plan.json`, SHA `d2a3f563f523f6ddd3f073cc33956cdef89f3cc0e0a3a8c4ac1c76322ceda245`. Its supplemental request SHA is `23fc528c749ad28c5b1f2ccdae5712ff1c7e508bc9e304ba8843743e8a86c6a5`. No frozen historical artifact is edited. The six source cases and their full gold remain unchanged in the new plan. Expected dispositions remain three accepted and three rejected.

Use Sonnet4.6 adaptive high with no temperature, 16000 shared tokens, connect3s/read75s, SDK total attempts1 and region us-east-1. The schema remains a closed `audits` object, now with only required key `0`; its sole row has reason before verdict. Local validation rejects blank reasons, reasons above600 codepoints, extra/missing/duplicate fields or identities, invalid types and verdicts. Only exact accepted originals may be selected; no repair, content return, alternative key or rewriting is possible.

Success requires all six native/local end_turn results within bounds,6/6 exact dispositions,3/3 sound retained,3/3 bad rejected, every reason accurate and grounded, and all accepted payloads byte-preserved. Independent review covers every reason. Any uncertain verdict fails. All failed or unattempted cases remain in the six-case denominator. No after-output relabeling or criterion changes.

Maximum six dispatches, with a fresh exclusive capture and no resumption. No retries, warmups, replacements, rescues or extra calls. Stop the whole trial on its first provider failure, non-end_turn, native schema failure or strict local validation failure. Semantic misses remain recorded and do not generate replacement calls. Credentials stay in memory. Provider reasoningContent text/signatures are removed from saved evidence, with only block counts retained. Record every request hash, strict replay, time and token usage.

This is an item-isolation diagnostic, not production qualification. Removing neighbors also changes workload and schema cardinality; any difference cannot be uniquely attributed to inter-item contamination. The historical batched outputs are a nonconcurrent reference, not a randomized simultaneous baseline. These are known cases, not new holdouts. Even perfect singleton results do not demonstrate worker yield or fit the current generation call budget: six audited items use six final-stage calls. Earlier failed trials remain failed. No production prompt/model/routing/default changes are authorized. The parent must separately approve this exact frozen plan hash before dispatch.
