# Unfrozen longer-read Sonnet constructed-worker trial

**Preparation only: UNFROZEN and unrun.** Root must review the exact final draft before freezing or authorizing any provider dispatch. No inference, account change, deployment, bank write, warmup, cache flush or rescue is authorized by this preparation.

The completed Sonnet baseline remains a failed trial: plan `6230622117349a2617e7502def86d625bd70fb401b82c3dd8f59c2a048624dcc`, capture `9402ce571dee1c386aa3489e548b82292408e9b4a984a4807d596a6ca982a707`. It returned nine of fifteen after its first author timed out. That call supplied no native output or usage; neither its task quality nor its hidden work is known. Preserve both artifacts and every one of their 44 source pins, including the earlier 37-pin baseline.

Candidate source is the separate clean checkout `/tmp/checkpoint-worker-response-timeout-200` at `77f794efa8cc24f94123cda61532ae7fe30d3e38`. The only runtime delta is the configured read-timeout upper bound 100→200 seconds plus its explanatory deadline comment. The harness compares the full candidate `question_generation.py` to that exact transform of the frozen baseline file. All other 26 runtime modules, requirements, SDK validator and bounded capture helper must be byte-identical; all old paths remain independently pinned alongside all new paths. No prior frozen source is modified or replaced.

The sole environment delta is `BEDROCK_READ_TIMEOUT_SECONDS=200`; the plan's `read_ceiling` also changes from 100 to 200. All author, solver and auditor requests retain Sonnet 4.6 adaptive/high, 16,000 shared tokens and no sampling fields. All prompts, native schemas, constructed-quantitative author mode, authored-solution policy and scope remain unchanged. The actual client factory still computes a smaller timeout from the remaining worker deadline, connect allowance, client setup and safety margin. This trial changes a maximum, not a promise that every stage gets 200 seconds.

Run the same three fixed five-item jobs once through the actual `_generate_sanitized_questions`: numerical, Python expressions and mixed arithmetic/English. Keep all **15 requested slots**, minimum difficulty two and the original mixed allocation. Each independent job retains its **240-second deadline and six durable call reservations**, with **18 dispatches maximum** overall. Connect timeout remains three seconds and total SDK attempts one. Runtime alone owns bounded top-ups, deduplication, ordinary failure handling and partial returns. A new mixed author pass conservatively requires three available calls. There is no new fallback, repair, retry, replacement job or resume.

Unchanged prospective requirements:

- At least 14/15 returns and four per job within the actual worker and provider budgets.
- At least six compiled returns, including four numerical and two mixed; mixed also needs at least one correctly assigned English prose item.
- Every returned literal task has sufficient premises, exactly one correct key, meaningfully different and plausible choices, sound displayed teaching, assignment fit, appropriate difficulty and novelty. Uncertainty fails. Do not invent an intended minimum, grammatical preference, omitted execution step or other condition to rescue a result.
- All five compiled fields recompile exactly from their trusted original task/specification through release at policy 8. Prose retains its exact authored main and empty choice feedback at policy 7. Only private validated compiler provenance can skip the model solver; every surviving item receives the immutable final audit.
- Preserve failed and unattempted slots in the original denominator. Native structure and model flags never substitute for independent learner-content review. No passing format check qualifies the trial by itself.

All existing integrity and capture safeguards remain: pre-import runtime/helper pins; baseline and candidate hashes at dispatch and completion; exact native source-built request/model configuration; actual per-call transport checks; durable reservations and request persistence before Converse; exclusive capture/no resume; external-capture-change detection; bounded safe errors; raw and decoded JSON credential-echo checks before storing or adapting text; preserved duplicate-member scanning; omitted reasoning/signatures; and unknown usage for missing responses. Ordinary provider/model/deadline failures follow the runtime and later independent jobs continue. Credential/setup, source/request/provenance, capture-integrity or actual-budget failures stop globally. Do not turn malformed JSON into a setup failure simply because the credential scanner cannot parse it.

Offline tests retain the previous 31 meaningful guard groups. Adapt their actual timeout expectations, then add exact-source-delta rejection, a simulated author taking longer than 100 but less than 200 seconds followed by an audit that fits the original 240-second deadline, and an author deadline overrun that cannot acquire an audit dispatch or yield credit. Existing late-final-response, shrinking timeout, 18-call, partial/top-up, constructor insufficiency, source-identity and safe-capture tests remain. Synthetic provider judgments are transport fixtures, never semantic evidence.

After completion, project returned stems and rotated choices with the unchanged key-independent worksheet helper, lock independent solutions, then unblind keys, teaching and model judgments. Report structural replay, compiler uptake, literal content, remaining quotas, per-stage timing and known token usage separately.

This is a fresh run on repeated scopes. It does not establish cold-cache causation: the actual provider grammar-cache state and the timed-out call's hidden progress were not observed. The existing grammars are reused without warmup or cache manipulation, and missing timeout output cannot establish why it was slow. A result can qualify only the frozen candidate under these narrow criteria; it cannot erase previous failures, prove universal semantics, attribute a matched causal effect or authorize production rollout.
